# Handoff — 27 May 2026, 16:19 (Sprint cierre — SetBonus cósmico runtime + Variants por zona + Vael Lanza Luz + Lyss reflexión + Items FUEGO/AGUA)

**Sesión:** Sprint pequeño tras handoff 15:51. Wireado SetBonus cósmicos en runtime (LUZ heal + VIENTO speed + SOMBRA mult duration + chances per element), skill variants assignment por zona (Leo decisión), Boss Vael 5to patrón Lanza de Luz Penetrante (Line2D tracking), 6 items dedicados FUEGO/AGUA per-zone, Boss Lyss Muralla Estática refleja proyectiles del player (REQ-INFRA grande resuelto).
**Fase del proyecto:** 3 — Lógica casi cerrada al 100%. Solo visual estético pendiente para TODAS las zonas.
**Generado por:** sesión Claude continuación post-handoff 15:51.

---

## 1. Qué se hizo en esta sesión (post-15:51)

### 1.1 SetBonusSystem cósmicos runtime ✅
Decisión Leo: implementar prioritario. Estado: ANTES data canon sin efecto runtime → AHORA fully effective.

- `HitboxComponent` + `Projectile`:
  - `_get_element_status_chance(elem)` — solo team=1, query SetBonusSystem. VIENTO 3pc → `chance × viento_desequilibrio_chance_mult_3pc`. SOMBRA 3pc → `chance × sombra_miasma_chance_mult_3pc`.
  - `_get_bendicion_heal_pct()` — LUZ 3pc → `base × luz_bendicion_heal_mult_3pc`.
  - `_get_miasma_duration_mult()` — SOMBRA 2pc → `data.sombra_miasma_duration_mult_2pc`. Aplicado vía `duration_override` cuando MIASMA aplicado.
- `PlayerStatsComponent._apply_set_bonus`:
  - LUZ 2pc → `_update_luz_passive_regen(rate)` → `player.set_luz_passive_regen()`.
  - VIENTO 2pc → bump multiplicativo encima de `move_speed_mult` (componer sobre skill MOVE_SPEED_PCT existente).
- `player.gd`:
  - `_luz_passive_regen_per_sec` + `_luz_regen_accum` fields.
  - `set_luz_passive_regen(rate)` API.
  - `_tick_luz_passive_regen(delta)` — accumulator-based, cada 1.0s `health.heal()` si vivo y < max.

### 1.2 Skill variants por zona ✅
Decisión Leo: política "Por zona" + opción 3 (override en world._spawn_stage post-spawn).

- `world.gd`: const `ZONA_SKILL_VARIANTS` dict (zona → enemy_class → skill_path).
- `world._spawn_stage`: post-spawn de R2/R3 (skip R4 bosses), llama `_apply_zone_skill_variant(enemy, enemy_class)`.
- `world._apply_zone_skill_variant`: lookup zona activa via `StageManager.current_zone`, carga .tres si hay match, asigna `enemy._r2_skill_data` + reinicia `_skill_r2_cooldown` con nuevo `_r2_cd_min()`.

Mapping canon Leo:
- **Zona 2** (FUEGO): MELEE→giratorio, ARCHER→disparo_reactivo, MAGE→erupcion_terrestre. Tank canon Taunt.
- **Zona 3** (AGUA): MELEE→tajo_doble, TANK→gancho_ascendente, MAGE→nova_hielo. Archer canon Ráfaga.
- **Zona 4** (VIENTO/LUZ): MELEE→salto_asalto, ARCHER→disparo_reactivo, MAGE→rafaga_arcana. Tank canon Taunt.
- **Zona 1**: sin override (canon R2_SKILL_RESOURCES en enemy.gd).

### 1.3 Boss Vael — Lanza de Luz Penetrante (5to patrón) ✅
REQ-INFRA grande resuelto. Line2D tracking lento.

- Estados `BOSS_STATE_LANZA_WINDUP` (1.0s charge) → `BOSS_STATE_LANZA_ACTIVE` (2.0s sweep).
- `_lanza_aim_dir` lerp lento (`LANZA_TRACKING_LERP=1.8 rad/s`) hacia player. Player puede esquivar saliendo del cono.
- Line2D dinámico: `_spawn_lanza_line / _update_lanza_line / _despawn_lanza_line`. Dorado, width 16, length 700.
- Damage tick cada 0.3s via `PhysicsShapeQuery2D` rectangular (LANZA_LENGTH × LANZA_WIDTH×1.5) en dirección actual. `LANZA_DAMAGE_MULT = 0.8` base R3 mage damage.
- F2 only. Cooldown 13-18s. Prioridad alta en `_try_start_pattern` si distancia > PATADA_RANGE × 1.5.
- Vael ahora tiene **5 patrones canon completos**: Ráfaga Arcana + Patada Frontal + Destello Sanador + Salto Cegador F2 + Lanza Luz F2.

### 1.4 Boss Lyss — Muralla Estática refleja proyectiles (REQ-INFRA grande) ✅
- `Projectile.reflect(new_team, new_source, new_direction)` API. Swap team + source + opcionalmente direction. Reset `_pierce_hit_set`. Tinte modulate dorado.
- Estados `BOSS_STATE_MURALLA_WINDUP` (0.4s) → `BOSS_STATE_MURALLA_ACTIVE` (1.5s).
- Durante MURALLA_ACTIVE: cada frame `_check_muralla_reflect()` busca Projectiles team=1 dentro de `MURALLA_REFLECT_RADIUS=90px`. Refleja con `proj.reflect(2, self, new_dir)`. `_muralla_reflected_set` evita reflejar proyectil 2 veces.
- VFX: aura cyan GPUParticles2D durante activo.
- F2 only. Cooldown 15-20s. Prioridad alta cuando player apunta lejos.
- Lyss ahora tiene **6 patrones**: Látigo Helado, Nova Hielo, Triple Tiro, Vórtice Gravedad F2, Canto Helado F2 aura, Muralla Estática Refleja F2.

### 1.5 Items dedicados FUEGO + AGUA per-zone ✅
6 items R3 nuevos:
- **FUEGO** (zona 2 reforzada): `arco_brasas` (BOW), `vara_brasas` (STAFF).
- **AGUA** (zona 3 base completa): `espada_lamento` (SWORD), `arco_glacial` (BOW), `cota_glacial` (ARMOR), `escudo_glacial` (SHIELD).

Esto cubre weapons de las 4 clases para FUEGO+AGUA. Antes FUEGO tenía solo SWORD/HAMMER; ahora también BOW/STAFF. AGUA tenía solo STAFF; ahora SWORD/BOW/STAFF + armor + shield dedicados.

### 1.6 Doc updates
- `habilidades_generales.md` §4.2.5 nueva — tabla variants por zona canon Leo.

---

## 2. En qué estamos ahora

- **Fase 3 lógica:** cerrada al ~95%. Items + skills + statuses + 7 bosses (Guardián/Duelista/Cazadora/Heraldo/Ignis/Lyss/Vael) + 6 elementos + 4 zonas todos canon completo.
- **Repo:** sucio. Acumulado total ~90 archivos nuevos entre sesiones. ~30 modificados.
- **Tests:** 4 archivos nuevos no ejecutados (status_effect_component_test, player_skill_system_test, element_status_synergy_test, element_system_test extendido). Leo debe correr headless.

### 2.1 Decisiones tomadas

- **Política variants por zona** (no por rareza/random/spawn) — Leo decisión.
- **GDD §5.3 sync diferido** post-playtest — Leo decisión.
- **Wireado SetBonusSystem via query helpers** (no runtime vars) — más simple, sin sincronización stale.
- **Vael Lanza Luz tracking lerp 1.8 rad/s** — esquivable pero no trivial.
- **Lyss Muralla refleja con tinte dorado** — legibilidad para player ("mi flecha vuelve a mí").

### 2.2 Decisiones pendientes Leo

- **Lyss boss healing en reflect** — actualmente el proyectil reflejado NO cura Lyss aunque tenga element LUZ. Considerar wireado vampire heal sobre proyectiles reflejados.
- **Vael status synergy** — al reflejar proyectil LUZ con `source=Lyss`, ¿Lyss recibe heal de su propio proyectil reflejado? (probable corner case, no testeado).
- **Skill variants Tank zona 2/4** — tabla canon no asigna variant para Tank en zona 2/4 (queda canon Taunt). Tank en zona 3 sí tiene Gancho Ascendente. Verificar si Leo quiere variant per zona para Tank también.
- **Tests integration** — necesitan scene tree mocks (SetBonusSystem, FuriaComponent, MomentumSystem autoloads). Tests puros actuales solo validan canon static.

---

## 3. Qué sigue

### 3.1 Próximo paso

**Validar compile + correr tests headless (Godot 4.6 local):**
```bash
godot --headless --script res://tests/systems/element_system_test.gd
godot --headless --script res://tests/systems/status_effect_component_test.gd
godot --headless --script res://tests/systems/player_skill_system_test.gd
godot --headless --script res://tests/systems/element_status_synergy_test.gd
godot --headless --script res://tests/systems/r2_skills_test.gd
godot --headless --script res://tests/systems/capstone_skills_test.gd
```

In-game smoke tests:
1. **Zona 2 skill variants**: `StageManager.set_zone(2)`. Enemies MELEE deben usar Corte Giratorio. Archers Disparo Reactivo. Mages Erupción Terrestre.
2. **Vael 5to patrón**: Zona 4 boss → F2 trigger en 50% HP → ver Lanza Luz Line2D tracking + tick damage.
3. **Lyss Muralla refleja**: Zona 3 boss F2 → cast Muralla Estática → tirar fireball player → ver proyectil reflejado (tinte dorado) volver hacia player.
4. **SetBonus cósmico LUZ**: equipar 2 items LUZ (casulla_luz + halo_luz) → +1.5 HP/s regen pasivo. Equipar 3 (con vara_luz) → BENDICION_HEAL 5%→10% per hit LUZ.
5. **SetBonus SOMBRA**: equipar 2 items → MIASMA dura 5s→7.5s. Equipar 3 → chance MIASMA on-hit 30%→45%.

### 3.2 Backlog lógico restante

1. **Tests integration cosmic statuses** — scene tree mocks de SetBonusSystem + FuriaComponent. Validar runtime application.
2. **Lyss healing en proyectil reflejado** (edge case) — wire opcional vampire heal cuando proyectil LUZ reflejado golpea.
3. **UI MainMenu selector zona** — wirear `StageManager.set_zone(N)` button. API existe, falta UI.
4. **Sistema logros / achievements** — track kills, primeras vez bosses, etc. Nueva feature.
5. **Tutorial integrado** — UI explicaciones in-game para sistemas RPG.
6. **Coliseo Fase 4 prep** — backend estructural sin servidor real.
7. **Tank skill variants zona 2 + zona 4** — actualmente canon Taunt, considerar variant.
8. **Wirear items dedicados FUEGO/AGUA en drop tables zona 2/3** — los items existen pero no son droppeables aún. Editar `zona2_boss_items.tres` + `zona3_boss_items.tres`.

### 3.3 Backlog visual (TODAS las zonas)

Sin cambios respecto handoff 15:51:
- Arte IA + sprites + parallax + audio por zona.
- VFX por status.
- HUD chips player skills + cooldowns + Furia meter.
- UI loadout player skills.

---

## 4. Contexto necesario para próxima sesión

### 4.1 SetBonus query pattern
- HitboxComponent + Projectile usan helpers `_get_element_status_chance / _get_bendicion_heal_pct / _get_miasma_duration_mult`.
- Solo team=1 (player) consulta SetBonusSystem. Enemies/bosses no tienen sets activos.
- SetBonusData fields cósmicos: `viento_move_speed_pct_2pc`, `viento_desequilibrio_chance_mult_3pc`, `luz_passive_hp_regen_2pc`, `luz_bendicion_heal_mult_3pc`, `sombra_miasma_duration_mult_2pc`, `sombra_miasma_chance_mult_3pc`.

### 4.2 Skill variants
- World.ZONA_SKILL_VARIANTS const dict: `{2: {0: path, 2: path, 3: path}, 3: {...}, 4: {...}}`.
- Override en `world._spawn_stage` → `_apply_zone_skill_variant`.
- Skip R4 bosses (tienen patrones propios).

### 4.3 Projectile reflect
- `proj.reflect(new_team, new_source, new_direction = ZERO)`.
- Solo Lyss usa actualmente. Otras skills futuras pueden reusar (Tank R3 Set A Muralla Estática).
- Source tracking compatible con LUZ vampire heal (heal new_source si proyectil reflejado LUZ pega).

### 4.4 Bosses state ints
- Guardian 100s, Duelista 200s, Ignis 300s, Lyss 400s, Vael 500s. Mini-boss / nuevos: 600+.

---

## 5. Archivos clave esta sesión (post-15:51)

### Creados
| Tipo | Archivos |
| :--- | :--- |
| Items FUEGO+AGUA | `resources/items/weapons/{arco_brasas,vara_brasas,espada_lamento,arco_glacial}.tres`, `armor/cota_glacial.tres`, `shields/escudo_glacial.tres` (6) |

### Modificados
| Archivo | Cambio |
| :--- | :--- |
| `scripts/components/hitbox_component.gd` | Query helpers SetBonus (chance/heal/duration mult) + duration_override en MIASMA |
| `scripts/projectiles/projectile.gd` | Mismo pattern + `reflect()` API |
| `scripts/components/player_stats_component.gd` | LUZ passive regen + VIENTO move speed bump compuestos |
| `scripts/entities/player.gd` | `_luz_passive_regen_per_sec` field + setter + tick |
| `scripts/world/world.gd` | ZONA_SKILL_VARIANTS const + `_apply_zone_skill_variant` post-spawn |
| `scripts/entities/boss_vael.gd` | Estados LANZA_WINDUP/ACTIVE + tracking lerp + Line2D + tick damage |
| `scripts/entities/boss_lyss.gd` | Estados MURALLA_WINDUP/ACTIVE + `_check_muralla_reflect` + aura cyan |
| `.claude/docs/habilidades_generales.md` | §4.2.5 tabla variants por zona |

---

## 6. Estado Fase 3 (27/05 16:19)

```
✅ Sistema StatusEffect genérico (12 statuses)
✅ Element synergy doble eje + 6 elementos canon
✅ SetBonus cósmicos VIENTO/LUZ/SOMBRA runtime efectivo
✅ Skill variants por zona wireados
✅ EnemySkillData Resource + dispatch por id (16 skills .tres total)
✅ 10 skills pool §4 implementadas
✅ Projectile vampire heal + reflect API
✅ PlayerSkillSystem (Furia → habilidad, 6 skills + save loadout)
✅ Zona 1 completa (canon GDD)
✅ Zonas 2, 3, 4 lógica completas (4 stages packs + 3 bosses propios + boss Vael 5 patrones reales)
✅ 7 bosses reales (Guardián, Duelista, Cazadora, Heraldo, Ignis, Lyss, Vael)
✅ ShieldComponent.add_temporary_charges + consume_charge_force
✅ HitboxComponent.ignore_shield + reflection
✅ HurtboxComponent FRACTURA consume + invuln direccional
✅ 15 items elementos completos (FUEGO/AGUA/TIERRA/VIENTO/LUZ/SOMBRA)
✅ 6 set bonuses (FUEGO/AGUA/TIERRA + VIENTO/LUZ/SOMBRA) — todos efectivos runtime
✅ Mini-boss stage type + banner UI
✅ World/StageManager multi-zona

⏹ Tests integration con scene tree
⏹ UI MainMenu selector zona (semi-visual)
⏹ Sistema logros / achievements (nuevo)
⏹ Tutorial integrado (visual+UI)
⏹ Coliseo Fase 4 prep
⏹ Visual TODAS las zonas (arte/sprites/parallax/audio/VFX)
⏹ HUD player skills + Furia meter
⏹ GDD §5.3 sync (post-playtest)
⏹ Drop tables zona 2/3 wiring items dedicados FUEGO/AGUA
```

58 tasks acumuladas total. Fase 3 lógica ~95% cerrada. Próxima dirección: tests + UI + sistemas nuevos (logros / Coliseo) o validación playtest.
