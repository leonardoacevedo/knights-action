# Handoff — 26 May 2026, 17:39 (Fase 3 lógica cerrada)

**Sesión:** Cierre completo de la lógica de Fase 3 — Set Bonuses, capstones, Sistema Oro, SaveSystem, Pool R3 armor/escudo, element cableado.
**Fase del proyecto:** 3 — Sistemas RPG Completos (LÓGICA cerrada, solo visual pendiente).
**GDD versión vigente:** v2.1
**Generado por:** /genera-resumen

---

## 1. Qué se hizo en esta sesión

### Task #1 — Pool R3 armor/escudo (4 items nuevos)
- `resources/items/armor/manto_raices.tres` — R3 TIERRA, stat_main 46, afijos hp+def+atkspd
- `resources/items/armor/peto_brasas.tres` — R3 FUEGO, stat_main 43
- `resources/items/shields/baluarte_terreo.tres` — R3 TIERRA, stat_main 35
- `resources/items/shields/aegis_igneo.tres` — R3 FUEGO, stat_main 32
- Drop tables editadas: `resources/stages/drop_tables/zona1_etapa_5_items.tres` (+2 entries TIERRA), `zona1_etapa_boss_items.tres` (+4 entries)
- Fix tildes/eñes en displays/descripciones + "tres cargas"→"dos cargas" (R3 escudo `block_charges() = 2`).

### Task #2 — Element armor/escudo → hurtbox
- `scripts/components/player_stats_component.gd` — agregado `_apply_defender_element(armor, shield)`. Prioridad: armor > shield > NEUTRO.
- `tests/systems/player_stats_component_test.gd` — +4 tests (armor propagates, shield cuando no armor, armor manda sobre shield, unequip resetea a NEUTRO).

### Task #3 — Set Bonuses 2pc/3pc afinidad elemental
- `scripts/systems/set_bonus_system.gd` (autoload nuevo) — `compute_affinity`, `refresh`, `is_active`, `get_bonus_data`. Regla: count piezas por elemento (excluyendo NEUTRO); max >= 2 → afinidad activa.
- `scripts/data/set_bonus_data.gd` (Resource) — schema con damage_multiplier_2pc, furia_regen_per_sec_2pc, hp_multiplier_2pc, fuego_aoe_*, agua_dash_reset_on_pass, tierra_block_restores_charge.
- `resources/set_bonuses/set_bonus_{fuego,agua,tierra}.tres` (3 archivos):
  - **FUEGO:** 2pc +10% daño · 3pc "Brasa Persistente" (AoE 80px / 60% daño al matar enemy)
  - **AGUA:** 2pc +1 Furia/s · 3pc "Corriente Eterna" (dash reset si pasa por enemy)
  - **TIERRA:** 2pc +15% HP máx · 3pc "Raíz Profunda" (al bloquear, +1 carga escudo cap original)
- Componentes editados: `hitbox_component.gd` (damage_set_bonus_multiplier), `furia_component.gd` (passive_regen API), `shield_component.gd` (tierra_3pc_active), `dash_component.gd` (agua_3pc_active + detection_area), `player.gd` (FUEGO 3pc kill detection + AoE), `inventory_screen.gd` (sección AFINIDAD).
- `tests/systems/set_bonus_test.gd` — 14 tests.

### Task #4 — Capstones árbol skills (7 TODOs resueltos)
- Stats cableados en componentes (todos del enum salvo CRIT_PCT que queda placeholder Fase 4+):
  - DASH_COOLDOWN_PCT, IFRAMES_PCT → `dash_component.gd`
  - MOVE_SPEED_PCT → `player.gd` (`move_speed_mult`)
  - EVADE_PCT → `hurtbox_component.gd` (RNG en `receive_hit`, cap 75%)
  - BLOCK_CHARGES → `shield_component.gd` (`skill_charges_bonus`)
  - FURIA_MAX / FURIA_GAIN_PCT / FURIA_REGEN_FLAT → `furia_component.gd`
  - ELEMENTAL_ADV_MULT → `hitbox_component.gd` (`elemental_adv_skill_bonus`)
- Capstones con efecto custom implementados:
  - **agil_golpe_tras_dash** — buff temporal +10% daño 0.5s post-dash
  - **agil_sombra_del_valle** — invisibilidad 0.5s (modulate.a=0.35) + ventaja elemental garantizada en primer golpe (`hitbox.force_elem_advantage`)
  - **guerrero_espiritu_marcial** — +20% daño 2s tras bloquear (`_espiritu_marcial_active`)
  - **mago_ventaja_aguzada** — +15% al multiplicador de ventaja elemental
  - **mago_resonancia_arcana** — +1 Furia/s pasiva (hook FuriaComponent reusado del Set Bonus AGUA)
- **Bug fix:** `agil_golpe_tras_dash` effect cambiado de DAMAGE_PCT (stat=2) a nuevo `POST_DASH_DAMAGE_PCT` (stat=16). Antes hacía double-count: el +10% se aplicaba permanente vía PlayerStatsComponent Y temporal post-dash. Ahora solo temporal.
- `scripts/data/skill_effect.gd` — agregado `POST_DASH_DAMAGE_PCT` al final del enum.
- `scripts/ui/skill_tree_screen.gd` — labels para todos los stats nuevos.
- `tests/systems/capstone_skills_test.gd` — 8 tests headless + 4 [INTEGRACION] marcados.

### Task #5 — Sistema Oro real
- `scripts/systems/gold_system.gd` (autoload nuevo) — `get_gold`, `can_afford`, `add`, `consume` (atómico), `register_enemy`, signals `gold_changed`/`not_enough_gold`.
- `scripts/systems/game_config.gd` — `gold_for_kill(rarity)`: R1=5, R2=15, R3=40, R4=200. Multiplica por Momentum (1+0.1×level, cap ×2).
- `scripts/systems/upgrade_manager.gd` — `REFINE_TABLE` con costos reales por nivel (+1=10g hasta +10=4000g, total cumulative +1→+10 ≈ 8270g). Validación + consumo de oro antes de tocar materiales.
- `scripts/systems/crafting_system.gd` — validación oro en `can_craft` y consumo atómico en `try_craft`. Reason "insufficient_gold" en abort.
- `scripts/systems/player_progression.gd` — `RESPEC_GOLD_COST = 500`. Respec valida oro + materiales atómico.
- `scripts/world/world.gd` — `GoldSystem.register_enemy` por enemy spawneado.
- `scripts/ui/hud_combat.gd` — `gold_label` minimal (null-safe; nodo a agregar en .tscn en Fase 5 polish).
- `resources/recipes/craft_*.tres` (5 archivos) — `gold_cost` real: R2=200g, R3/boss=800g.
- `tests/systems/gold_system_test.gd` — 19 tests.

### Task #6 — SaveSystem persistencia entre cierres
- `scripts/systems/save_system.gd` (autoload nuevo, último en `[autoload]`) — `request_save` debounced, `save_now`, `load`, `delete_save`, `has_save`. Write atómico `.tmp` + rename. Schema versionado (`CURRENT_VERSION = 1`) con hook migration.
- `scripts/systems/player_progression.gd` — `_serialize_state()` + `_restore_state(dict)`.
- `scripts/systems/inventory_system.gd` — `_serialize_state()` + `_restore_state(dict)` + `_serialize_item(item)` + `_deserialize_item(dict)`. Items se reconstruyen desde `base_path` + override de `refinement_level` + afijos.
- `scripts/systems/gold_system.gd` — `_serialize_state()` + `_restore_state(dict)`.
- Persistencia: `user://savegame.json`. Persiste nivel/XP/skill_points/unlocked_nodes/items/materials/equipped/gold. NO persiste HP, momentum, stage actual.
- Save automático en signals: `level_up`, `skill_unlocked`, `respec_done`, `equipped_changed`, `materials_changed` (debounced), `refine_succeeded/failed`, `craft_succeeded`, `gold_changed` (debounced), `stage_cleared`.
- `tests/systems/save_system_test.gd` — 14 tests (round-trip, schema, migration, ItemData con afijos).

### Hotfixes finales (compile check headless)
- `scripts/entities/player.gd:505` — `var collider := hit.get("collider")` tipado explícito `Object` (warning-as-error de Variant inference).
- `scripts/components/player_stats_component.gd:281` — `var script := sibling.get_script()` tipado explícito `Script` con cast.
- Re-corrida headless: **compile + runtime limpio**.

---

## 2. En qué estamos ahora

- **Fase y hito:** Fase 3. La **LÓGICA está cerrada al 100%**. Solo falta lo visual: arte IA, sprites, parallax, audio, Momentum HUD polish, label de oro en hud_combat.tscn. Hito declarado "una zona completa es jugable end-to-end" — alcanzable cuando lo visual entre.
- **Última feature tocada:** SaveSystem persistencia.
- **Estado del repo:** sucio. ~21 archivos modificados + 8 untracked nuevos (gold_system, save_system y sus tests). Otros archivos creados (set_bonuses, items R3, capstone_skills_test, etc.) ya están tracked pero sin diferencias respecto a git por estado raro de OneDrive — verificar antes de commit.
- **Tests:** ~70 tests nuevos sumados (4 element + 14 set_bonus + 8+4 capstone + 19 gold + 14 save). Tests headless siguen sin cargar autoloads (limitación canon pre-existente). Tests de fórmula pura corren OK; los [INTEGRACION] requieren editor.
- **Compile + runtime headless:** **limpio** (verificado con `--check-only` y `--quit-after 2`).

### Decisiones tomadas en esta sesión
- **R3 pool elementos:** 2 piezas TIERRA + 2 piezas FUEGO (no 1+1) para que Set Bonuses tenga opciones de build.
- **Element priority (defender):** armor > shield > NEUTRO. Justificación: armor cubre más cuerpo.
- **Set Bonus regla afinidad:** arma + armadura + escudo. Mayor count de un elemento (excluyendo NEUTRO); >=2 → afinidad activa.
- **Set Bonus 2pc/3pc valores:** confirmados con Leo (recommended): FUEGO +10%/AoE · AGUA +1F/s/dash reset · TIERRA +15%HP/restore charge.
- **POST_DASH_DAMAGE_PCT (nuevo stat):** evita double-count en `agil_golpe_tras_dash`. No se aplica permanente.
- **Sistema Oro:** Momentum SÍ multiplica oro (≠ XP). Persiste entre Game Over (es del personaje). Reset solo en Nueva Partida.
- **SaveSystem:** single slot JSON `user://savegame.json`. Write atómico. Debounce 0.5s. NO persiste HP/momentum/stage.

### Decisiones pendientes (esperan a Leo)
- **`martillo_guardian.tres` tiene `rarity = 3` (R4)** pero GDD dice "R4 excluido MVP". Bug o intencional. NO tocado.
- **SaveSystem reset on death:** propuesta = NO borrar al Game Over (alineado con XP/Oro). Solo "Nueva Partida" explícita. Confirmar.
- **Single slot save:** confirmar. Multi-slot queda post-Fase 3.
- **Label "Oro: N" en hud_combat.tscn:** `gold_label` ya conectado vía script (null-safe). Falta agregar el nodo Label en el .tscn desde el editor. Delegable a Fase 5 visual.

---

## 3. Qué sigue

- **Próximo paso recomendado:** abrir Godot, correr proyecto, probar end-to-end:
  1. Matar enemies → ver "+N Oro" + XP toasts.
  2. Equipar item, refinar (verifica costo oro), craftear (verifica costo oro), respec (verifica costo 500g).
  3. Cerrar Godot, reabrir → save debe cargar (nivel, skills, inventario, oro persisten).
  4. Probar Set Bonuses: equipar 2 FUEGO → ver +10% daño en HUD/stats. Equipar 3 FUEGO → al matar enemy, próximo golpe AoE.
  5. Probar capstone: dash + atacar dentro de 0.5s → +10% daño solo en ese golpe (no permanente).

- **Backlog Fase 3 restante (solo visual):**
  1. Arte IA por zona (Midjourney/SD/Nano Banana via `art-prompt-engineer`).
  2. Sprites de player, enemies, boss.
  3. Parallax background zona 1.
  4. Audio pipeline (música zona + combate, SFX por evento).
  5. Momentum HUD final (visual definitivo).
  6. Label de oro en hud_combat.tscn.
  7. Set Bonus indicator en HUD durante combate (no solo InventoryScreen).

- **Cosas a verificar manualmente (Leo, en Godot):**
  - **Save round-trip completo:** matar, equipar, refinar, cerrar Godot → reabrir → ¿todo persiste?
  - **Set Bonus 3pc TIERRA:** ¿bloqueo no se queda en 0 con escudo R3? Recupera +1 carga.
  - **Sombra del Valle:** ¿modulate.a=0.35 visible 0.5s post-dash?
  - **Espíritu Marcial:** ¿daño extra al atacar dentro de 2s tras bloquear?
  - **Oro económico:** ¿alcanza para algunos refinamientos sin sentir grind exagerado?

---

## 4. Contexto necesario para la próxima sesión

- **Stat enum nuevo:** `SkillEffect.Stat.POST_DASH_DAMAGE_PCT` (índice 16) es **buff temporal**, NO se cablea permanente en `PlayerStatsComponent`. Solo `player.gd._on_dash_ended` lo lee.
- **3 autoloads nuevos** registrados en `project.godot`: `SetBonusSystem` (después de ExperienceSystem), `GoldSystem` (después de SetBonusSystem), `SaveSystem` (último — depende de los otros).
- **SaveSystem write atómico:** `user://savegame.json.tmp` → `DirAccess.rename`. Si Godot crashea mid-write, save previo intacto.
- **InventorySystem.\_serialize\_item:** cada item se serializa como `{base_path, refinement_level, affixes}`. Restore reconstruye desde el .tres base y aplica overrides. **Importante:** si el path del .tres base cambia (refactor / renombre), saves viejos rompen ese item con `push_warning` (no crash).
- **Tests headless NO cargan autoloads** (lección canon vigente). Tests de fórmula pura OK; tests con `GameConfig.*` o `InventorySystem.*` deben usar instancia directa con override `_inv`.
- **HudCombat.gold_label es opcional** — el script verifica `if gold_label != null` antes de actualizar. Agregar el Label en el .tscn es Fase 5.
- **PowerShell `[char]9` para tabs** si Edit pierde indentación (lección canon).
- **`block_charges()`:** R1=0, R2=1, R3=2, R4=3. Los escudos R3 dicen "dos cargas" en su descripción (no tres).

---

## 5. Archivos clave de esta sesión

### Creados
| Archivo | Razón |
| :--- | :--- |
| `resources/items/armor/manto_raices.tres` | R3 armor TIERRA |
| `resources/items/armor/peto_brasas.tres` | R3 armor FUEGO |
| `resources/items/shields/baluarte_terreo.tres` | R3 escudo TIERRA |
| `resources/items/shields/aegis_igneo.tres` | R3 escudo FUEGO |
| `resources/set_bonuses/set_bonus_{fuego,agua,tierra}.tres` | Data set bonus 2pc/3pc |
| `scripts/data/set_bonus_data.gd` | Resource schema set bonus |
| `scripts/systems/set_bonus_system.gd` | Autoload afinidad |
| `scripts/systems/gold_system.gd` | Autoload economía oro |
| `scripts/systems/save_system.gd` | Autoload persistencia |
| `tests/systems/set_bonus_test.gd` | 14 tests |
| `tests/systems/capstone_skills_test.gd` | 8+4 tests |
| `tests/systems/gold_system_test.gd` | 19 tests |
| `tests/systems/save_system_test.gd` | 14 tests |

### Modificados (claves)
| Archivo | Cambio |
| :--- | :--- |
| `project.godot` | +3 autoloads (SetBonusSystem, GoldSystem, SaveSystem) |
| `scripts/components/player_stats_component.gd` | _apply_defender_element, _apply_set_bonus, capstone stats, _serialize_state ya NO (lo hace inventory) |
| `scripts/components/{hitbox,hurtbox,furia,dash,shield}_component.gd` | Nuevos campos para set bonus + capstones |
| `scripts/data/skill_effect.gd` | +POST_DASH_DAMAGE_PCT, sacar TODO de stats cableados |
| `scripts/entities/player.gd` | Set Bonus 3pc FUEGO + buffs post-dash + espíritu marcial + fix Variant |
| `scripts/systems/upgrade_manager.gd` | REFINE_TABLE gold real + consumo |
| `scripts/systems/crafting_system.gd` | Validación + consumo oro |
| `scripts/systems/player_progression.gd` | RESPEC_GOLD_COST=500 + _serialize/_restore |
| `scripts/systems/inventory_system.gd` | _serialize/_restore + serialize_item con afijos |
| `scripts/systems/game_config.gd` | gold_for_kill |
| `scripts/world/world.gd` | GoldSystem.register_enemy hook |
| `scripts/ui/hud_combat.gd` | gold_label minimal (null-safe) |
| `scripts/ui/skill_tree_screen.gd` | Labels stats nuevos + POST_DASH_DAMAGE_PCT |
| `resources/skills/nodes/agil/agil_golpe_tras_dash.tres` | stat 2→16 (fix double-count) |
| `resources/recipes/craft_*.tres` (5) | gold_cost real |
| `resources/stages/drop_tables/zona1_etapa_{5,boss}_items.tres` | +R3 armor/escudo |
| `tests/systems/player_stats_component_test.gd` | +4 tests element propagation |

---

## Estado Fase 3 (actualizado)

```
✅ Zona 1 completa (6 stages)
✅ Sistema Elementos (backend + asignación zona 1)
✅ Árbol de Skills (30 nodos + UI + TODOS los stats cableados + capstones implementados)
✅ XP-on-kill (toast + banner + persistencia)
✅ Element armor/escudo → hurtbox (defensa elemental real)
✅ Pool R3 armor/escudo (4 items nuevos)
✅ Set Bonuses 2pc/3pc afinidad (3 elementos)
✅ Sistema Oro real (drop + refinamiento + crafteo + respec)
✅ SaveSystem persistencia entre cierres (JSON atómico versionado)

⏹ Arte IA + sprites + parallax (visual)
⏹ Audio pipeline (visual/audio)
⏹ Momentum HUD final (visual)
⏹ Label oro en hud_combat.tscn (visual minimal)
```
