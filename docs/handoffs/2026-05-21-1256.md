# Handoff — 21 de Mayo 2026, 12:56

**Sesión:** Bootstrap completo de `.claude/` + 5 reglas de trabajo + sistema de handoffs.
**Fase del proyecto:** Fase 1 — Prototipo de Combate.
**GDD versión vigente:** v2.1.
**Generado por:** /genera-resumen (primera ejecución del sistema).

---

## 1. Qué se hizo en esta sesión

### Reemplazo del GDD
- `knights_action_gdd_v2.md` borrado.
- `GDD.md` creado en raíz con contenido completo de v2.1 (incluye §5.6 Sistema de Refinamiento +1 a +10 con probabilidades de fallo y Pergaminos de Protección).

### Estructura de carpetas creada
- `.claude/{agents,commands,docs}/`
- `docs/{features,handoffs}/`
- `scripts/{systems,data}/` (las que faltaban)
- `resources/`
- `tests/`

### CLAUDE.md raíz creado
- 13 secciones cubriendo identidad, reglas, pilares, estado actual (Fase 1), stack, mapa, flujo operativo, agentes/comandos, glosario, fórmulas, anti-patrones.
- Sección §2 dedicada a las 5 reglas operativas de Leo (no negociables).

### 14 subagentes especializados en `.claude/agents/`
- `game-designer.md` (opus) — guardián de pilares y scope.
- `godot-expert.md` (sonnet) — GDScript 4.6.
- `combat-system.md` — Momentum, Furia, dash, bloqueo.
- `equipment-system.md` — items, rarezas, refinamiento.
- `progression-system.md` — XP, árbol de skills, respec.
- `enemy-ai.md` — mobs R1-R3 con state machines.
- `boss-designer.md` (opus) — bosses R4 con telegrafía.
- `ecos-coliseum.md` — Coliseo, 4 perfiles IA, Gloria.
- `balance-engineer.md` (opus) — simulaciones DPS/TTK/economía.
- `level-designer.md` — zonas con 5-8 etapas + boss.
- `ux-mobile.md` — HUD, hit areas ≥44dp, layout móvil.
- `art-prompt-engineer.md` — template maestro para IA generativa.
- `narrative-lore.md` — naming, descripciones, bestiario.
- `backend-architect.md` (opus) — Firebase/Supabase + anti-cheat.

### 10 slash commands en `.claude/commands/`
- `/feature`, `/design-item`, `/design-enemy`, `/design-boss`, `/design-skill`, `/balance-check`, `/pillar-check`, `/status`, `/new-zone`, `/playtest-log`.

### 8 docs auxiliares en `.claude/docs/`
- `reglas-de-trabajo.md` — las 5 reglas con ejemplos y diagrama de flujo.
- `pilares.md` — los 4 pilares con ejemplos resueltos.
- `arquitectura.md` — composición sobre herencia + capas.
- `convenciones-gdscript.md` — naming, tipado, plantillas.
- `glossary.md` — todos los términos del juego.
- `formulas.md` — todas las fórmulas matemáticas + changelog.
- `fases.md` — plan 1→5 con hitos.
- `estructura-carpetas.md` — mapa completo del repo.

### READMEs en carpetas nuevas
- `docs/features/README.md`, `scripts/components/README.md`, `scripts/systems/README.md`, `scripts/data/README.md`, `resources/README.md`, `tests/README.md`.

### Sistema de reglas operativas (5 reglas)
- Sección §2 prominente en `CLAUDE.md`.
- Doc detallado en `.claude/docs/reglas-de-trabajo.md`.
- Reglas: (1) consultar antes de eliminar, (2) avisar si algo podría romper el juego, (3) preguntar si hay dudas, (4) siempre español, (5) avisar mejoras o riesgos en vez de hacerlo solo.

### Sistema de handoffs (este mismo handoff es la primera ejecución)
- `.claude/commands/genera-resumen.md` — slash command.
- `docs/handoffs/README.md` — convenciones.
- `docs/handoffs/2026-05-21-1256.md` (este archivo).
- `docs/handoffs/LATEST.md` — copia del más reciente.
- Hook `SessionStart` en `.claude/settings.json` para carga automática (próxima sesión lo prueba).

### Configuración
- `.claude/settings.json` con permisos preaprobados (Read libre, Bash básico, git lectura, Godot CLI no destructivo) y denies fuertes (rm -rf, force push, .env).

---

## 2. En qué estamos ahora

### Fase y hito
- **Fase 1: Prototipo de Combate.**
- **Hito a alcanzar:** "Pelear se siente bien."
- **% estimado hacia el hito:** ~30%. Player tiene movimiento + dash con i-frames + ataque básico + Furia acumulándose. Falta la mitad core (Momentum, Bloqueo, Decay de Furia, telegrafía+ataque del dummy, feedback de impacto).

### Estado del repo
- **Sin git inicializado** (no es repo git). Todos los cambios viven en filesystem.
- Estructura nueva consolidada y sin restos del GDD viejo.
- Código de juego intacto: `scripts/player.gd`, `scripts/components/health_component.gd`, escenas existentes — NO se tocaron en esta sesión.

### Decisiones tomadas en esta sesión
- GDD v2.1 reemplaza v2.0 (incluye Refinamiento).
- Setup `.claude/` lo más amplio posible (14 agentes vs los 13 originalmente planificados — se agregó `ux-mobile` como pieza independiente).
- Todo en español, alineado con el GDD.
- 5 reglas operativas formalizadas (consulta antes de borrar, avisar riesgos, preguntar dudas, español, proponer mejoras antes de aplicar).
- Sistema de handoffs con archivo histórico + LATEST.md y hook SessionStart.
- "Eliminar bloques grandes de código" en regla #1 = funciones enteras, clases, secciones lógicas de 5+ líneas o exports de Resources.
- Reglas solo en el proyecto, NO en memoria persistente global de Claude.

### Decisiones pendientes (esperan a Leo)
- ¿Arrancar Fase 1 con `MomentumSystem` (autoload nuevo) o con refactor de `player.gd` a componentes (Hitbox/Hurtbox/Furia/Dash/Block)?
- ¿Inicializar git ahora o más adelante?

---

## 3. Qué sigue

### Próximo paso recomendado
Implementar el **Sistema de Momentum** como autoload (`scripts/systems/momentum_system.gd`) — es la pieza CORE que falta de Fase 1 según GDD §4.3.

Trigger: `/feature sistema de momentum`.

### Backlog corto de Fase 1
1. **MomentumSystem** (autoload) con señales y multiplicadores.
2. **Refactor de `player.gd`** para integrar Momentum (hit landed → +1, damage taken → reset 0).
3. **BlockComponent** + escudo equipable con cargas.
4. **FuriaComponent** con decay (-5/s tras 5s sin atacar) — extraer del actual `player.gd`.
5. **Telegrafía + ataque del enemy_dummy** (para sentir riesgo y ver reset de Momentum).
6. **Feedback de impacto:** hitstop 30-80ms, screenshake, partículas básicas, SFX placeholder.
7. **Feedback de Momentum ≥5x:** color del contador, partículas alrededor del player.

### Cosas a verificar manualmente (Leo, en Godot)
- Abrir el proyecto en Godot 4.6 y confirmar que la escena `world.tscn` sigue corriendo después de los cambios al filesystem (no toqué `.tscn` ni scripts de juego, no debería romperse, pero confirmá).
- Confirmar que el ícono nuevo `.claude/` aparece en el FileSystem panel de Godot (debería aparecer como carpeta normal — Godot la ignora porque empieza con `.`).

---

## 4. Contexto necesario para la próxima sesión

- **Reglas operativas críticas** (las 5) están en `CLAUDE.md` §2 y `.claude/docs/reglas-de-trabajo.md`. Aplican a Claude principal y a todos los subagentes.
- **El hook SessionStart** debería volcar este LATEST.md automáticamente al inicio. Si no aparece, leerlo manualmente: `Get-Content docs/handoffs/LATEST.md`.
- **El proyecto NO está bajo git todavía.** Hasta que se inicialice, no usar comandos git en commands o agentes (revisarlos si Leo lo confirma).
- **No hay tests todavía.** El primer test será para `MomentumSystem` cuando se implemente.
- **GDD.md es source of truth absoluto** — si encuentro contradicción con código existente, gana GDD salvo que Leo apruebe lo contrario.

---

## 5. Archivos clave de esta sesión

| Archivo | Cambio | Razón |
| :--- | :--- | :--- |
| `GDD.md` | nuevo | Source of truth de diseño (v2.1, reemplaza v2.0). |
| `knights_action_gdd_v2.md` | borrado | Versión obsoleta — v2.1 ya incluye su contenido. |
| `CLAUDE.md` | nuevo | Guía operativa primaria para Claude (13 secciones, 14k chars). |
| `.claude/settings.json` | nuevo | Permisos, env vars, hook SessionStart. |
| `.claude/agents/*.md` (14) | nuevos | Subagentes especializados por dominio. |
| `.claude/commands/*.md` (11) | nuevos | Slash commands del proyecto. |
| `.claude/docs/*.md` (8) | nuevos | Docs auxiliares (reglas, pilares, fórmulas, etc.). |
| `docs/features/README.md` | nuevo | Convenciones de documentación de features. |
| `docs/handoffs/README.md` | nuevo | Convenciones del sistema de handoff. |
| `docs/handoffs/2026-05-21-1256.md` | nuevo | Este handoff. |
| `docs/handoffs/LATEST.md` | nuevo | Copia del más reciente (entrypoint para próxima sesión). |
| `scripts/{components,systems,data}/README.md` | nuevos | Convenciones por capa. |
| `resources/README.md` | nuevo | Convenciones para .tres. |
| `tests/README.md` | nuevo | Convenciones de tests + plantillas. |
