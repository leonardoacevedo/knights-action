# Handoff — 27 May 2026, 07:40 (Bosses 4/4 + Menú + Taunt MMO real)

**Sesión:** Boss hitboxes proporcionales, Skills R2 por clase, Subset A skills + AoeTelegraph, Menú principal + Modo Prueba arena, 3 nuevos bosses (Duelista/Cazadora/Heraldo), categoría Boss en menú prueba, taunt rework completo a MMO real, catálogo `habilidades_generales.md`.
**Fase del proyecto:** 3 — Sistemas RPG Completos (LÓGICA + BOSSES cerrado al 100%, solo visual pendiente).
**GDD versión vigente:** v2.1
**Generado por:** /genera-resumen

---

## 1. Qué se hizo en esta sesión

### Task #7 — Boss hitboxes proporcionales (Guardián)
- `scenes/entities/boss_guardian.tscn` — override shapes: BodyShape 32×108, Hitbox 72×54 @ (50,-50), Hurtbox 44×112 @ (0,-56). Sprite escala 2.0× ahora coincide con colisiones reales.

### Task #8 — Skills R2 por clase (4 nuevas)
- `scripts/entities/enemy.gd` — +2 states (`R2_SKILL_TELEGRAPH`, `R2_SKILL_ATTACK`), `R2_SKILL_TABLE` const, lógica por clase (Archer ráfaga 3 spread, Melee embestida-dash+knockback, Mage canalizar 1.2s+fireball ×1.8 ×1.5, Tank Taunt damage-soak). R3 hereda R2 (dual cooldown).
- `scripts/components/hurtbox_component.gd` — `taunt_soaker` redirect.
- `scripts/entities/player.gd` — `apply_external_velocity(push)` para knockback del Melee R2.
- `tests/systems/r2_skills_test.gd` — 9 tests puros.

### Task #9 — Subset A skills + infra AoeTelegraph
- `scripts/effects/aoe_telegraph.gd` + `scenes/effects/aoe_telegraph.tscn` — infra reutilizable (`setup(radius, duration, color)`).
- `scripts/projectiles/projectile.gd` — flags `aoe_on_impact`, `pierce_enemies`, `pierce_count` + trails Line2D.
- Mage R1 Orbe Flamígero (fireball + AoE 35px/60% al impactar). Archer R1 Flecha Perforante (pierce ×3). Guerrero R3 Sed de Sangre (3er cooldown, buff +20% vel/atk 5s, CD 12s).
- **Bug fix double-count:** `agil_golpe_tras_dash` effect cambiado de `DAMAGE_PCT` a nuevo `POST_DASH_DAMAGE_PCT` (stat=16). Antes se aplicaba permanente + temporal.
- `scripts/data/skill_effect.gd` — agregado `POST_DASH_DAMAGE_PCT`.
- `tests/systems/projectile_skills_test.gd` — 10 tests.

### Task #10 — Menú principal + Modo Prueba arena
- `scenes/ui/main_menu.tscn` + `scripts/ui/main_menu.gd` — StickFigure central con equipo + 5 botones (Historia, Pruebas, Nueva Partida, Inventario, Skills).
- `scenes/ui/test_arena_config.tscn` + `scripts/ui/test_arena_config_screen.gd` — configurador por clase × rareza × cantidad (cap 8 mobs).
- `scripts/systems/test_arena_config.gd` (autoload) — `is_test_mode` + `pending_spawns`.
- `experience_system.gd`/`gold_system.gd`/`drop_system.gd` — guards early-return si `is_test_mode` (aislamiento).
- `world.gd._ready` — si `is_test_mode`, construye StageData runtime con spawns custom.
- `hud_combat.gd` — botón "MENU" top-right discreto.
- `project.godot` — main_scene → main_menu.tscn + autoload TestArenaConfig.

### Task #11 — 3 nuevos bosses (Melee/Archer/Mage)
- **Duelista de las Cenizas** (Melee/FUEGO) — `boss_duelista.gd` (497 líneas) + `.tscn` + drop table. 5 patrones: Tajo doble, Embestida ígnea, Parry Stance (contraataque ×2 si lo golpean), Combo Final, Llamarada en Arco (fase 2). HP 60% del Guardián, velocidad 1.5×.
- **Cazadora del Crepúsculo** (Archer/AGUA) — `boss_cazadora.gd` (557 líneas) + `.tscn` + drop table. Triple posición elevada, teleporta cada 8-12s. 5 patrones: Flecha Pesada, Lluvia Vertical, Lluvia AoE, Tele-shot, Mareo Frío (fase 2, slow). HP 70%.
- **Heraldo del Vacío** (Mage/NEUTRO) — `boss_heraldo.gd` (425 líneas) + `.tscn` + drop table. AoEs persistentes. 5 patrones: Orbe del Vacío, Campo de Daño, Onda Expansiva, Mini-Meteoros, Eco Eterno (fase 2, 2 hazards permanentes). HP 80%, velocidad 0.5×.
- `scripts/effects/persistent_hazard.gd` + `.tscn` — infra zonas dañinas con duración/tick (usada por Heraldo).
- `player.gd` — `apply_slow(mult, duration)` + `_external_speed_mult` + `_external_speed_timer` para Mareo Frío.
- 3 drop tables `resources/stages/drop_tables/boss_{duelista,cazadora,heraldo}_items.tres` — R3 + chance R4.

### Task #12 — Categoría Boss en Modo Prueba
- `test_arena_config_screen.gd` — 5ta fila "BOSS ÚNICO" en dorado, selector clase (radio Melee/Tank/Archer/Mage) + toggle Sin/Con Boss. Contador independiente "Mobs: N/8 · Boss: M/1".
- `world.gd` — @export para 3 nuevas boss scenes + helper `_boss_scene_for_class()` mappea cualquier R4 a su boss según clase. Removido check `data.is_boss` (cualquier R4 usa boss scene).

### Ajustes complementarios
- **Mage R1 / Archer R1 heredan a R2/R3:** filtro `rarity == R1` → `rarity != R4`. R2/R3 mage también disparan Orbe Flamígero en su ataque básico (skill R2 grande va por path separado y NO recibe AoE). Idem Archer Flecha Perforante.
- **Melee R3 → 3 skills** (no 4): quitada skill genérica R3 (`SKILL_TELEGRAPH`) para Melee — era redundante con ataque básico. Quedó: ataque básico + Embestida R2 + Sed de Sangre R3.
- **Taunt rework completo en 2 iteraciones:**
  - Nerf inicial: 50% → 30% redirect (Leo dijo "es overkill como el canalizar mage").
  - Leo: "el taunt sigue sin ser taunt real": 30% → **100%** redirect + marker "!" pulsante + flash rojo pantalla + damage floater sobre tank + aliado parpadea blanco.
  - Leo: "debe atraer al player": **override de input** — `player.set_taunt_source(tank)` fuerza facing hacia el tank + pull horizontal a SPEED completa hasta 35px (player conserva salto/ataque/bloqueo/dash). Decisión Leo: "no importa que se sienta roto, debe ser así".
- **2 hotfixes Variant inference:** `player.gd:505` (`collider := hit.get("collider")` → tipado `Object`), `player_stats_component.gd:281` (`script := sibling.get_script()` → tipado `Script`). `test_arena_config_screen.gd:289` (`col := RARITY_COLORS[idx]` → `Color`).

### Doc nuevo
- `.claude/docs/habilidades_generales.md` (~250 líneas) — catálogo único de skills: filosofía (Animation Lock + telegrafía + roles), estado por rareza con regla R1→R2→R3 explícita, infra disponible/parcial/grande/inviable, pool Set A/B/C con viabilidad, §5 skills player (sistema pendiente), §8 pendientes futuros.

---

## 2. En qué estamos ahora

- **Fase y hito:** Fase 3. **Lógica + bosses + menú cerrado al 100%**. Único pendiente: visual (arte IA, sprites, parallax, audio, momentum HUD polish, label oro en hud_combat.tscn).
- **Última feature tocada:** Taunt MMO real (force facing + pull) + sección §8 pendientes futuros en doc.
- **Estado del repo:** sucio. 13 modified + 36 untracked nuevos (3 bosses + scenes + drop tables + AoeTelegraph + PersistentHazard + menú + arena config + tests).
- **Tests:** ~80 tests nuevos en esta sesión (9 R2 skills + 10 projectile skills + boss phases + arena). Headless siguen sin cargar autoloads (limitación canon). Tests de fórmula puros OK.
- **Compile + runtime headless:** **limpio**. Warnings legacy de `savegame.json` con base_path vacío persisten (skip silencioso, no rompe — bug del SaveSystem inicial a investigar después).

### Decisiones tomadas en esta sesión
- **Boss visual proporcional:** factores BodyShape ×1.6/×1.8, Hitbox ×1.8, Hurtbox ×2.0. Solo override en `boss_guardian.tscn`, `enemy.tscn` intacto.
- **Skills R2/R3:** R3 hereda R2 + suma propia. Dual cooldown independiente.
- **POST_DASH_DAMAGE_PCT:** stat dedicado para evitar double-count de DAMAGE_PCT.
- **R1→R2→R3:** mejoras de ataque básico se heredan a rarezas superiores. Skills R2/R3 separadas NO reciben las del R1 (evita overkill stacking).
- **Melee R3 = 3 skills:** quitada genérica redundante.
- **Modo Prueba aislado:** XP/oro/drops no se acumulan (guards en 3 sistemas).
- **Cap arena prueba:** 1 boss + 8 mobs (contadores independientes).
- **Boss scenes route:** cualquier R4 + clase → boss específico (Tank=Guardián, Melee=Duelista, Archer=Cazadora, Mage=Heraldo).
- **Identidades bosses:** Duelista (parry stance), Cazadora (3 posiciones elevadas), Heraldo (AoEs persistentes sin minions). 2 fases en 50% HP. Drop R3 + chance R4.
- **Taunt MMO real:** override de input del player aceptado por Leo. "Forced look at enemy" removido de §3 ⛔ del doc.
- **Set Bonus AGUA 2pc + skill mago_resonancia_arcana:** ambos usan `passive_regen` de FuriaComponent. Suman.

### Decisiones pendientes (esperan a Leo)
- **`martillo_guardian.tres`** sigue con `rarity = 3` (R4). GDD dice "R4 excluido MVP". Pendiente confirmar o cambiar.
- **`savegame.json` legacy** con `base_path` vacío genera warnings al cargar. Si querés clean → `SaveSystem.delete_save()` en consola.
- **Botón MENU en HUD:** agregado por ux-mobile como conveniencia para testing. Si en producción no querés, hacer condicional con `GameConfig.DEBUG_*`.

---

## 3. Qué sigue

- **Próximo paso recomendado:** abrir Godot, probar end-to-end:
  1. Menú principal aparece al arrancar.
  2. Modo Pruebas → seleccionar TANK boss + 2 mobs Melee R1 → validar taunt MMO real (facing + pull obligatorios).
  3. Probar los 4 bosses (1 a la vez): Duelista (parry), Cazadora (teleport 3 pos), Heraldo (AoEs persistentes), Guardián (ya validado antes).
  4. Validar herencia R1→R2/R3: Mage R2 ataque básico debería explotar con AoE; Archer R3 debería atravesar enemies.
  5. Validar Melee R3 alterna SOLO entre Embestida (R2) y Sed de Sangre (R3) — no skill genérica.
- **Backlog Fase 3 restante (solo visual):**
  1. Arte IA por zona (Midjourney/SD/Nano Banana via `art-prompt-engineer`).
  2. Sprites player + enemies + 4 bosses.
  3. Parallax background zona 1.
  4. Audio pipeline (música zona + combate, SFX por evento).
  5. Momentum HUD final.
  6. Label oro en hud_combat.tscn (`gold_label` ya conectado vía script, null-safe — solo agregar nodo).
  7. Set Bonus indicator en HUD durante combate.
- **Pendientes documentados §8 `habilidades_generales.md`** (post visual):
  - Revisar TODAS las skills con más detalle (feel, balance, edge cases).
  - Sistema de skills activas del jugador (Furia → efecto).
  - StatusEffect system genérico.
  - Migración EnemySkill a Resource .tres.

### Cosas a verificar manualmente (Leo, en Godot)
- **Taunt MMO real:** el player es **forzado** a mirar + acercarse al tank. ¿Se siente bien o demasiado fuerte? (Leo dijo "no importa que se sienta roto" — confirmar igual).
- **Bosses spawneables desde Modo Prueba:** Heraldo + AoEs persistentes (¿player tiene espacio para moverse?), Cazadora teletransporta limpio.
- **Drop tables nuevas:** cada boss dropea items distintos.

---

## 4. Contexto necesario para la próxima sesión

- **Catálogo de skills:** `.claude/docs/habilidades_generales.md` es ahora el source of truth de skills. Antes de implementar/modificar skill nueva, editar primero el doc.
- **`POST_DASH_DAMAGE_PCT` (stat 16):** buff temporal, NO se cablea permanente. Solo `player.gd._on_dash_ended` lo lee. Si necesitás un patrón similar para otro skill, copiar este approach.
- **`player.set_taunt_source(tank)`:** API para que cualquier skill futura pueda forzar facing+pull del player. Pasar null para liberar.
- **`AoeTelegraph` + `PersistentHazard`:** infra creada. Reusar para skills futuras (Salto de Asalto, Nova de Hielo, Erupción Terrestre, Trampa, Muro de Llamas, etc.).
- **TestArenaConfig:** autoload + flag `is_test_mode` + `pending_spawns`. Guards en ExperienceSystem/GoldSystem/DropSystem ya aplican.
- **World boss route:** cualquier entry con `rarity == R4` y `enemy_class` válido se mappea automáticamente a la boss scene correspondiente. Para escapar de este comportamiento (R4 genérico sin boss), necesitará flag explícito (a futuro).
- **Lecciones canon vigentes:**
  - Edit pierde tabs → PowerShell `[char]9`.
  - Variant inference con warnings-as-errors → tipar explícito `var X: Type = ...` para indexar constantes/dicts.
  - Tests headless NO cargan autoloads (limitación pre-existente).
  - Triángulo elemental se calcula en el atacante.
  - Stats = Base + Equipo + Skills + Set.

---

## 5. Archivos clave de esta sesión

### Creados
| Archivo | Razón |
| :--- | :--- |
| `.claude/docs/habilidades_generales.md` | Catálogo único skills + viabilidad técnica |
| `scenes/effects/aoe_telegraph.{tscn,gd}` | Infra reutilizable para AoEs telegrafiadas |
| `scenes/effects/persistent_hazard.{tscn,gd}` | Zonas dañinas con duración/tick (Heraldo) |
| `scripts/entities/boss_duelista.{gd,tscn}` | Melee boss FUEGO |
| `scripts/entities/boss_cazadora.{gd,tscn}` | Archer boss AGUA |
| `scripts/entities/boss_heraldo.{gd,tscn}` | Mage boss NEUTRO |
| `resources/stages/drop_tables/boss_{duelista,cazadora,heraldo}_items.tres` | Loot R3 + chance R4 |
| `scenes/ui/main_menu.{tscn,gd}` | Menú principal con 5 botones |
| `scenes/ui/test_arena_config.{tscn,gd}` | Configurador Modo Prueba |
| `scripts/systems/test_arena_config.gd` | Autoload con flag + spawns |
| `tests/systems/r2_skills_test.gd` | Tests skills R2 |
| `tests/systems/projectile_skills_test.gd` | Tests pierce/AoE/Sed Sangre |
| `tests/bosses/boss_phases_test.gd` | Tests fase 2 de 3 nuevos bosses |

### Modificados (claves)
| Archivo | Cambio |
| :--- | :--- |
| `project.godot` | main_scene → main_menu + autoload TestArenaConfig |
| `scripts/entities/enemy.gd` | +800 líneas: R2_SKILL_TABLE, states R2_*, AoeTelegraph hook, Sed Sangre, Taunt MMO real |
| `scripts/entities/player.gd` | `apply_external_velocity`, `apply_slow`, `set_taunt_source`, override input en `_handle_input` |
| `scripts/components/hurtbox_component.gd` | `taunt_soaker` redirect 100% + flash aliado + damage floater al tank |
| `scripts/projectiles/projectile.gd` | aoe_on_impact, pierce_enemies, trails |
| `scripts/world/world.gd` | Boss route por clase + TestArenaConfig hook |
| `scripts/systems/{experience,gold,drop}_system.gd` | Guards is_test_mode |
| `scripts/ui/hud_combat.gd` | Botón MENU + gold_label opcional |
| `scenes/entities/boss_guardian.tscn` | Hitboxes proporcionales |
| `resources/skills/nodes/agil/agil_golpe_tras_dash.tres` | stat 2→16 (fix double-count) |
| `scripts/data/skill_effect.gd` | +POST_DASH_DAMAGE_PCT |

---

## Estado Fase 3 (actualizado)

```
✅ Zona 1 completa (6 stages)
✅ Sistema Elementos (backend + asignación zona 1)
✅ Árbol de Skills (30 nodos + capstones cableados)
✅ XP-on-kill (toast + banner + persistencia)
✅ Element armor/escudo → hurtbox
✅ Pool R3 armor/escudo (4 items)
✅ Set Bonuses 2pc/3pc
✅ Sistema Oro real
✅ SaveSystem persistencia entre cierres
✅ Boss hitboxes proporcionales (Guardián)
✅ Skills R2 por clase (4 nuevas)
✅ Skills factibles subset A + AoeTelegraph
✅ Menú principal + Modo Prueba arena (aislada)
✅ 4 bosses (Guardián + Duelista + Cazadora + Heraldo)
✅ Modo Prueba con categoría Boss
✅ Taunt MMO real (force facing + pull, override de input)
✅ Catálogo habilidades_generales.md

⏹ Arte IA + sprites + parallax (visual)
⏹ Audio pipeline (visual/audio)
⏹ Momentum HUD final (visual)
⏹ Label oro en hud_combat.tscn (visual minimal)
```
