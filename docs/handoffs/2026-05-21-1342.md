# Handoff — 21 de Mayo 2026, 13:42

**Sesión:** Bootstrap completo `.claude/` + 5 reglas + handoffs + skill caveman + flexibilización regla #4.
**Fase del proyecto:** Fase 1 — Prototipo de Combate.
**GDD versión vigente:** v2.1.
**Generado por:** /genera-resumen (2da ejecución; reemplaza handoff 12:56).

---

## 1. Qué se hizo en esta sesión

### A. Bootstrap inicial (capturado también en handoff 12:56)
- **GDD reemplazado:** `knights_action_gdd_v2.md` borrado → `GDD.md` con v2.1 completa (incluye §5.6 Sistema de Refinamiento).
- **Estructura nueva:** `.claude/{agents,commands,docs}/`, `docs/{features,handoffs}/`, `scripts/{systems,data}/`, `resources/`, `tests/`.
- **`CLAUDE.md` raíz:** identidad, reglas, pilares, estado Fase 1, stack, mapa, flujo operativo, glosario, fórmulas, anti-patrones.
- **14 subagentes especializados** en `.claude/agents/`: game-designer, godot-expert, combat-system, equipment-system, progression-system, enemy-ai, boss-designer, ecos-coliseum, balance-engineer, level-designer, ux-mobile, art-prompt-engineer, narrative-lore, backend-architect.
- **11 slash commands** en `.claude/commands/`: feature, design-item, design-enemy, design-boss, design-skill, balance-check, pillar-check, status, new-zone, playtest-log, genera-resumen.
- **9 docs auxiliares** en `.claude/docs/`: reglas-de-trabajo, estilo-caveman, pilares, arquitectura, convenciones-gdscript, glossary, formulas, fases, estructura-carpetas.
- **READMEs** en `docs/features/`, `scripts/{components,systems,data}/`, `resources/`, `tests/`.
- **`.claude/settings.json`** con permisos, env vars, hook SessionStart.

### B. Reglas operativas (5 + 1 = 6 reglas)
- **Reglas 1-5** documentadas en `CLAUDE.md` §2 y `.claude/docs/reglas-de-trabajo.md`:
  1. Crear/editar SÍ, eliminar PREGUNTA (incluye bloques grandes de código).
  2. Si puede romper algo, frená y avisá + proponé alternativas.
  3. Si tenés dudas, preguntá (no asumas salvo que esté explícito en `.md`).
  4. ~~Siempre español~~ → **flexibilizada** (ver punto D).
  5. Si tenés mejora o complemento, avisá y preguntá antes.
- **Regla 6 agregada:** Default caveman (estilo de comunicación). Detalle en `.claude/docs/estilo-caveman.md`.

### C. Sistema de handoffs
- `.claude/commands/genera-resumen.md` — slash command que tipeás "genera resumen".
- `docs/handoffs/README.md` — convenciones.
- `docs/handoffs/2026-05-21-1256.md` — primer histórico (sesión hasta el momento del setup).
- `docs/handoffs/2026-05-21-1342.md` — este archivo, sesión completa.
- `docs/handoffs/LATEST.md` — siempre copia del histórico más reciente.
- **Hook `SessionStart`** en `.claude/settings.json` (matcher `startup|resume|clear`) que ejecuta `Get-Content docs/handoffs/LATEST.md` y vuelca al contexto inicial.

### D. Skill `caveman` instalado y configurado como default ★ NUEVO desde 12:56
- **Instalación:** `npx skills add https://github.com/juliusbrussee/caveman --skill caveman` (Leo lo ejecutó).
- **Archivos:** `.agents/skills/caveman/{SKILL.md, README.md}` + symlink `.claude/skills/caveman → .agents/skills/caveman/`.
- **Estado en esta sesión:** archivos presentes pero el harness aún NO lo registró como tool invocable (necesita restart de Claude Code).
- **`.claude/docs/estilo-caveman.md`** creado: doc completo con intensidad por contexto, ejemplos en español, auto-pausa, combinación con reglas, desactivación.
- **Intensidad default acordada:** `full` (sin artículos, fragmentos OK) en chat. `lite` (sin filler, gramática completa) en comentarios de código GDScript.
- **`CLAUDE.md` §2 Regla 6** agregada con scope detallado.
- **`convenciones-gdscript.md`** actualizada: sección "Comentarios" en caveman lite con ejemplo antes/después.
- **14 agentes** + **10 commands** modificados con directiva `> **Estilo de output:** caveman full por defecto...` al inicio. Casos especiales documentados:
  - `narrative-lore`: copy final del juego NO caveman.
  - `ux-mobile`: strings de UI NO caveman.
  - `art-prompt-engineer`: prompts MJ/SD intactos (sintaxis load-bearing).
  - `backend-architect`: auto-pausa para privacy/anti-cheat.
  - `/genera-resumen`: NO se tocó (handoffs deben ser legibles humanos en frío).
  - `/balance-check`: cambios numéricos en formato claro, no caveman.
  - `/playtest-log`: entrada en `docs/playtest/*.md` va normal.

### E. Regla #4 flexibilizada ★ NUEVO desde 12:56
- **Antes:** "Siempre español + única excepción nombres en código."
- **Después:** "Español por default, inglés cuando ahorra tokens reales."
- **Cambio a inglés OK** si: ack corto (Done, OK, Failed), terminología técnica densa (stack traces, paths, API names), o frase con ≥25% ahorro vs español.
- **Siempre español (no flexibilizable):** GDD, docs de referencia, `docs/features/*`, comentarios en `.gd`, copy de UI del juego, handoffs, commits/PRs.
- Editado en `CLAUDE.md` §2 y `.claude/docs/reglas-de-trabajo.md`.

---

## 2. En qué estamos ahora

### Fase y hito
- **Fase 1: Prototipo de Combate.**
- **Hito a alcanzar:** "Pelear se siente bien."
- **% estimado hacia el hito:** ~30%. Player con movimiento + dash + i-frames + ataque básico + Furia (sin decay todavía). Falta: Momentum, Bloqueo, decay Furia, telegrafía+ataque del dummy, feedback de impacto, feedback Momentum ≥5x.

### Estado del repo
- **NO está bajo git.** Todos los cambios viven en filesystem.
- Setup `.claude/` completo y consolidado. Estructura del proyecto lista para arrancar implementación de Fase 1.
- Código de juego **sin tocar** desde el setup: `scripts/player.gd`, `scripts/components/health_component.gd`, escenas existentes intactas.

### Decisiones tomadas en esta sesión
- GDD v2.1 reemplaza v2.0 (incluye Refinamiento).
- Setup `.claude/` amplio (14 agentes, 11 commands, 9 docs auxiliares).
- 6 reglas operativas formalizadas (5 originales + caveman default).
- Sistema de handoff con archivo histórico inmutable + LATEST.md sobrescribible.
- Hook SessionStart configurado para volcar LATEST.md al inicio automáticamente.
- Skill caveman instalado vía `npx skills add` con intensidad `full` default + `lite` en comentarios.
- Regla #4 flexibilizada con criterio claro (≥25% ahorro, acks cortos, técnico denso).
- Caveman aplica a chat y outputs de agentes; NO a docs de referencia, handoffs, copy de juego, código.

### Decisiones pendientes (esperan a Leo)
- ¿Arrancar Fase 1 con `MomentumSystem` (autoload nuevo) o con refactor de `player.gd` a componentes (Hitbox/Hurtbox/Furia/Dash/Block)?
- ¿Inicializar git ahora o más adelante?
- ¿Reiniciar Claude Code después de este handoff para validar que el skill caveman se cargue y el hook SessionStart funcione?

---

## 3. Qué sigue

### Próximo paso recomendado
**Reiniciar Claude Code en el repo** para validar end-to-end:
1. El hook `SessionStart` debería volcar este LATEST.md al contexto inicial automáticamente.
2. El skill caveman debería aparecer como tool registrada por el harness.
3. La regla #6 debería activarse → respuestas en caveman full por default.

Si todo OK, próximo paso técnico: `/feature sistema de momentum` (autoload `scripts/systems/momentum_system.gd`).

### Backlog corto de Fase 1
1. **MomentumSystem** (autoload) con señales y multiplicadores.
2. **Refactor de `player.gd`** para integrar Momentum (hit landed → +1, damage taken → reset 0).
3. **BlockComponent** + escudo equipable con cargas.
4. **FuriaComponent** con decay (-5/s tras 5s sin atacar) — extraer del actual `player.gd`.
5. **Telegrafía + ataque del enemy_dummy** (para sentir riesgo y ver reset de Momentum).
6. **Feedback de impacto:** hitstop 30-80ms, screenshake, partículas básicas, SFX placeholder.
7. **Feedback de Momentum ≥5x:** color del contador, partículas alrededor del player.

### Cosas a verificar manualmente (Leo, en Godot)
- Abrir el proyecto en Godot 4.6 y confirmar que `world.tscn` corre OK (no se tocaron scripts ni escenas de juego, debería estar intacto).
- Verificar que la carpeta `.claude/` y `.agents/` no rompan nada en el FileSystem panel (Godot ignora carpetas que empiezan con `.`).
- Probar al reiniciar Claude Code que el hook SessionStart muestre el contenido de LATEST.md.

---

## 4. Contexto necesario para la próxima sesión

- **6 reglas operativas críticas** están en `CLAUDE.md` §2 y `.claude/docs/reglas-de-trabajo.md`. Aplican a Claude principal y a todos los subagentes.
- **Regla #6 (caveman)** se aplica desde ahora — respuestas en chat van en caveman full. Si el skill no está cargado por el harness, igual aplico el estilo manualmente.
- **Regla #4 flexibilizada:** inglés OK si ahorra tokens reales (acks, técnico denso, ≥25%). Resto sigue español.
- **El hook SessionStart** vuelca LATEST.md automáticamente. Si por alguna razón no aparece en contexto, leelo manualmente con la tool Read antes de hacer cualquier otra cosa.
- **El proyecto NO está bajo git todavía.** No usar comandos git en commands o agentes hasta que Leo lo inicialice.
- **No hay tests todavía.** El primer test será para `MomentumSystem` cuando se implemente.
- **GDD.md es source of truth absoluto.** Si encuentro contradicción con código existente, gana GDD salvo aprobación explícita de Leo.
- **Caveman se desactiva con:** `normal mode` / `stop caveman` (temporal) o `/caveman lite|ultra` (cambia intensidad).
- **El hook PowerShell** del SessionStart asume entorno Windows. Si Leo migra a Mac/Linux, actualizar `.claude/settings.json`.

---

## 5. Archivos clave de esta sesión

| Archivo | Cambio | Razón |
| :--- | :--- | :--- |
| `GDD.md` | nuevo | Source of truth de diseño (v2.1). |
| `knights_action_gdd_v2.md` | borrado | Versión obsoleta. |
| `CLAUDE.md` | nuevo + ediciones | Guía operativa primaria. Reglas 1-6 en §2. |
| `.claude/settings.json` | nuevo + edición | Permisos, env vars, hook SessionStart. |
| `.claude/agents/*.md` (14) | nuevos + directiva caveman | Subagentes especializados con estilo caveman full. |
| `.claude/commands/*.md` (11) | nuevos + directiva caveman (10) | Slash commands. `/genera-resumen` sin caveman. |
| `.claude/docs/reglas-de-trabajo.md` | nuevo + edición regla #4 | 6 reglas operativas con ejemplos. |
| `.claude/docs/estilo-caveman.md` | nuevo | Intensidad por contexto, ejemplos español, auto-pausa. |
| `.claude/docs/convenciones-gdscript.md` | nuevo + edición comentarios | Style guide + comentarios en caveman lite. |
| `.claude/docs/{pilares,arquitectura,glossary,formulas,fases,estructura-carpetas}.md` | nuevos | Docs auxiliares de referencia. |
| `docs/features/README.md` | nuevo | Convenciones doc de features. |
| `docs/handoffs/README.md` | nuevo | Convenciones del sistema de handoff. |
| `docs/handoffs/2026-05-21-1256.md` | nuevo | Primer histórico (setup inicial). |
| `docs/handoffs/2026-05-21-1342.md` | nuevo | Este handoff (sesión completa). |
| `docs/handoffs/LATEST.md` | sobrescrito | Copia del 1342 (entrypoint próxima sesión). |
| `scripts/{components,systems,data}/README.md` | nuevos | Convenciones por capa. |
| `resources/README.md` | nuevo | Convenciones para `.tres`. |
| `tests/README.md` | nuevo | Convenciones de tests + plantillas. |
| `.agents/skills/caveman/{SKILL,README}.md` | instalado vía npx | Skill de comunicación comprimida. |
| `.claude/skills/caveman` | symlink | Vinculado al skill en `.agents/`. |

**Total archivos creados/modificados en la sesión:** ~50.
**Líneas de doc generadas:** ~3500.
**Código de juego tocado:** 0 (toda la sesión fue setup).
