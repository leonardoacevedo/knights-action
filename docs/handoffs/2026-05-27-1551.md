# Handoff — 27 May 2026, 15:51 (Sprint logico — Projectile vampire + Skills pool §4 + Items LUZ/SOMBRA/VIENTO + Boss Vael + Mini-boss flag)

**Sesión:** Sprint pequeño tras handoff masivo previo. Projectile vampire heal LUZ wireado, 10 skills nuevas del pool §4 implementadas (Corte Giratorio, Tajo Doble, Patada Frontal, Salto Asalto, Nova Hielo, Erupción Terrestre, Disparo Reactivo, Ráfaga Arcana, Gancho Ascendente, Arma Imbuida), 9 items elementos cósmicos + 3 set bonuses, boss Vael real 4 patrones, mini-boss stage type, tests element_status_synergy_test.gd.
**Fase del proyecto:** 3 — Sistemas RPG Completos. Lógica casi cerrada (sin visual estético). 48 tasks acumuladas en esta sesión + previa.
**GDD versión vigente:** v2.1 (código va por delante con 6 elementos canon + eje cósmico).
**Generado por:** sesión Claude continuación post-handoff 14:28.

---

## 1. Qué se hizo en esta sesión (post-14:28)

### 1.1 Projectile.source_entity + LUZ vampire heal
- `Projectile.gd`: nuevo campo `source_entity: Node` + `set_source(entity)` API. Setear antes de `add_child` al árbol.
- LUZ branch en `Projectile._try_apply_element_status` → `_apply_bendicion_heal_to_source()` cura source_entity 5% HP máx.
- 13 sites de `proj.launch(...)` actualizados con `proj.set_source(self)` (player, PlayerSkillSystem, enemy.gd ×4, boss_heraldo, boss_cazadora ×5, boss_lyss).

### 1.2 Tests element_status_synergy
- `tests/systems/element_status_synergy_test.gd` — 10 tests puros: Fractura id + magnitude canonical + consume on hit + no status no mult, Miasma id + INDEPENDENT stack + bypass armor health.take_damage path, Bendición id canonical, Desequilibrio id + magnitude canonical.
- Tests skip integraciones que requieren scene tree (enemy state cancel, bendición source resolution, miasma furia halve).

### 1.3 Skills pool §4 batch 1 — Corte Giratorio + Tajo Doble + Patada Frontal
- 3 nuevas EnemySkillData .tres en `resources/enemy_skills/r2_melee_{giratorio,tajo_doble,patada}.tres`.
- Dispatch en `_enter_r2_skill_attack / _tick_r2_skill_attack` por `_r2_skill_data.id` (skill-driven, no class-driven).
- Executors: `_enter_r2_giratorio / _tick_r2_giratorio` (hitbox activo + avance lento + multi-hit tick), `_enter_r2_tajo_doble / _tick_r2_tajo_doble` (2 hits secuenciales con gap), `_enter_r2_patada / _tick_r2_patada` (hitbox corto + knockback fuerte).
- Helper `_apply_patada_knockback` aplica `apply_external_velocity` al player si dentro de rango.

### 1.4 Skills pool §4 batch 2 — Salto Asalto + Nova Hielo + Erupción Terrestre
- 3 nuevas .tres: `r2_melee_salto_asalto`, `r2_mage_nova_hielo`, `r2_mage_erupcion_terrestre`.
- Salto Asalto: AoeTelegraph + jump arc (velocity.y JUMP + velocity.x toward target) + landing damage radial 75px + knockback.
- Nova Hielo: AoeTelegraph fijo bajo player + AoE damage 70px tras 0.55s + aplica FREEZE.
- Erupción Terrestre: AoeTelegraph + AoE damage 50px tras 0.5s + aplica FRACTURA (single-use vulnerabilidad).
- Helpers compartidos: `_r2_spawn_aoe_telegraph`, `_r2_apply_radial_damage`, `_r2_apply_radial_damage_with_status` (con status optional).

### 1.5 Skills pool §4 batch 3 — Disparo Reactivo + Ráfaga Arcana + Gancho Ascendente + Arma Imbuida
- 4 nuevas .tres: `r2_archer_disparo_reactivo`, `r2_mage_rafaga_arcana`, `r2_tank_gancho_ascendente`, `r2_melee_arma_imbuida`.
- Disparo Reactivo: 1 proyectil veloz (×2 speed, ÷2 dmg, scale 0.8).
- Ráfaga Arcana: 3 proyectiles secuenciales con `_r2_rafaga_next_shot_at` accumulator (delay 0.12s).
- Gancho Ascendente: hitbox + `ShieldComponent.consume_charge_force(2)` → fuerza romper 2 cargas de escudo del player aunque no esté bloqueando.
- Arma Imbuida: buff propio 5s. `HitboxComponent.ignore_shield = true` + timer en `_physics_process._tick_arma_imbuida_buff(delta)` para mantener buff cross-state. `HurtboxComponent.receive_hit` saltea `shield.try_absorb` si `source.ignore_shield`.

### 1.6 Infra nueva sumada
- `HitboxComponent.ignore_shield: bool` — flag para skip de escudo defensor.
- `ShieldComponent.consume_charge_force(n)` — destruye N cargas force-mode sin requerir is_blocking.
- `HurtboxComponent.receive_hit` — chequea `source.ignore_shield` antes de llamar `shield.try_absorb`.

### 1.7 Items dedicados elementos VIENTO/LUZ/SOMBRA (9 items R3)
- VIENTO: `arco_huracan` (BOW), `manto_viento` (ARMOR), `paves_viento` (SHIELD).
- LUZ: `vara_luz` (STAFF), `casulla_luz` (ARMOR), `halo_luz` (SHIELD).
- SOMBRA: `daga_sombra` (SWORD), `capa_sombra` (ARMOR), `orbe_sombra` (SHIELD).
- Todos R3 (rarity=2), affixes hp + def + atkspd según tipo.

### 1.8 Set Bonuses VIENTO/LUZ/SOMBRA
- `SetBonusData` enum extendido a 7 elementos + nuevos fields:
  - `viento_move_speed_pct_2pc`, `viento_desequilibrio_chance_mult_3pc`
  - `luz_passive_hp_regen_2pc`, `luz_bendicion_heal_mult_3pc`
  - `sombra_miasma_duration_mult_2pc`, `sombra_miasma_chance_mult_3pc`
- 3 .tres nuevos: `set_bonus_viento`, `set_bonus_luz`, `set_bonus_sombra`.
- **Pendiente:** wireado en `SetBonusSystem` (lectura de campos nuevos) + `PlayerStatsComponent.recalculate` (aplicación de bonuses). Por ahora data exists, runtime no aplica.

### 1.9 Boss Vael (Zona 4 LUZ R4 Mage)
- `boss_vael.gd` 280 líneas + `boss_vael.tscn`. 4 patrones reales:
  1. **Ráfaga Arcana** (3 proyectiles delay 0.12s)
  2. **Patada Frontal** (hitbox 110px + knockback 300/-180, gap creator)
  3. **Destello Sanador** (heal 8% HP, auto-cast F2 si HP ≤ 30%)
  4. **Salto Cegador** F2 (jump + landing AoE radius 90 + STUN player)
- HP 0.95× MAGE R4 base. Velocidad 1.6× (boss más rápido). Damage ×1.2 R3 base.
- Lanza de Luz Penetrante (5to patrón, laser tracking 2s) sigue REQ-INFRA pendiente.
- `zona4_etapa_boss.tres`: `boss_scene_override = ExtResource("5_vael")`.

### 1.10 Mini-boss stage type
- `StageData.is_mini_boss: bool` — nuevo flag.
- `stage_banner.gd` lee flag: muestra "MINI-BOSS — ETAPA N / M" con título dorado tamaño intermedio entre normal y BOSS.
- `zona4_etapa_3.tres`: `is_mini_boss = true` (Capitán de los Vientos).

### 1.11 Doc updates
- `habilidades_generales.md` §4.1/4.2/4.3 actualizada — 10 skills marcadas IMPL (27/05) con paths + executors.

---

## 2. En qué estamos ahora

- **Fase y hito:** Fase 3 lógica casi cerrada. 10 skills pool §4 IMPL, 6 elementos canon, 6 bosses reales (Guardián/Duelista/Cazadora/Heraldo/Ignis/Lyss/Vael — solo Vael nuevo esta sesión), 9 items + 3 sets cósmicos, Projectile vampire heal completo. Visual TODAS las zonas sigue pendiente.
- **Última feature tocada:** Mini-boss stage type + Vael wired.
- **Estado del repo:** sucio masivo. ~70 archivos nuevos acumulados entre sesiones (status_effects/, enemy_skills/, player_skills/, bosses Ignis/Lyss/Vael, materials/items/recipes/stages zonas 2-4, drop tables, set bonuses cósmicos, tests). ~25 modificados.
- **Tests:** 3 nuevos esta sesión (suite element_status_synergy con 10 cases) + extensión element_system_test. NO ejecutados.

### 2.1 Decisiones tomadas

- **Skill dispatch por id** (no por class): `_r2_skill_data.id` driven en `_enter_r2_skill_attack` permite variants per-clase sin agregar enemy_class nuevas.
- **HitboxComponent.ignore_shield** preferido sobre status effect (más performante, sin lifecycle).
- **ShieldComponent.consume_charge_force** sin requerir is_blocking — castiga incluso si player no estaba bloqueando activamente.
- **Arma Imbuida** mantiene timer cross-state via `_physics_process` hook (no solo durante R2_SKILL_ATTACK).
- **Vael 4 patrones (no 5)** — skip Lanza de Luz Penetrante (REQ-INFRA Line2D tracking). Destello Sanador rellena slot 3 como mecánica sustain.
- **Set bonuses cósmicos data-only por ahora**: SetBonusSystem + PlayerStatsComponent NO leen aún viento/luz/sombra fields. Pendiente wireado runtime.

### 2.2 Decisiones pendientes (Leo)

- **Wireado SetBonusSystem + PlayerStatsComponent** — leer + aplicar los nuevos fields cósmicos en runtime. Hoy solo data canon, no efectivos.
- **Bonus stat para hp_regen pasivo (LUZ 2pc)** — necesita timer en player.gd o HealthComponent que aplique heal periódico cada segundo.
- **Chance mult para Desequilibrio (VIENTO 3pc) y Miasma (SOMBRA 3pc)** — `HitboxComponent.ELEMENT_STATUS_CHANCE` es const. Para aplicar mult del set bonus, necesita pasar a runtime var seteado por `SetBonusSystem` cuando set 3pc activo.
- **Lanza de Luz Penetrante (Vael 5to patrón)** — REQ-INFRA Line2D tracking lento. ~40 líneas + tunear feel.
- **Tunear chances/cooldowns/damages** post-playtest:
  - Element status 30% chance puede ser mucho para LUZ vampire o poco para SOMBRA miasma.
  - Vael velocidad 1.6× puede ser overwhelming.
  - Arma Imbuida 5s duration puede ser largo si bypass armor stack con DESEQUILIBRIO.
- **Decidir qué enemies usan qué skill variant** — actualmente todas las skills nuevas existen como .tres pero NO se asignan a ningún enemy. R2_SKILL_RESOURCES sigue mapeando class → canon skill (Embestida/Taunt/Burst/Canalizar). Leo debe decidir per spawn entry o per zone.
- **GDD §5.3 oficial actualizar** — sigue con 3 elementos, código va con 6. Leo aprobar edición GDD.
- **Items dedicados** zona 2-4 — actualmente recetas crafteo zona 2/3 reusan items zona 1. Crear weapons/armors/shields temáticos zona 2 (FUEGO) y zona 3 (AGUA) dedicados (no solo FUEGO/AGUA placeholders).

---

## 3. Qué sigue

### 3.1 Próximo paso recomendado

**Validar compile + correr tests headless (Godot 4.6 local):**
```bash
godot --headless --script res://tests/systems/status_effect_component_test.gd
godot --headless --script res://tests/systems/player_skill_system_test.gd
godot --headless --script res://tests/systems/element_system_test.gd
godot --headless --script res://tests/systems/element_status_synergy_test.gd
godot --headless --script res://tests/systems/r2_skills_test.gd
godot --headless --script res://tests/systems/capstone_skills_test.gd
```

In-game smoke tests prioritarios:
1. **Skills variants asignación**: en debug console o test arena, swap `enemy._r2_skill_data` a `load("res://resources/enemy_skills/r2_melee_giratorio.tres")` y validar Corte Giratorio se ejecuta.
2. **Vael Zona 4 boss**: `StageManager.set_zone(4); change_scene → world.tscn`. Pelear hasta etapa 7 (boss) — validar 4 patrones, F2 trigger en 50% HP, Destello auto-cast en 30%, Salto Cegador STUN player.
3. **Items cósmicos drop/equip**: inventory debug add 9 items nuevos. Equipar set 3pc VIENTO o LUZ o SOMBRA, verificar SetBonusSystem reconoce afinidad (`set_bonus_*.tres` cargados).
4. **Mini-boss banner Zona 4 etapa 3**: validar UI muestra "MINI-BOSS — ETAPA 3 / 7" en dorado.
5. **Arma Imbuida bypass shield**: enemy con `_r2_skill_data = r2_melee_arma_imbuida.tres` cast skill, observar 5s sprite tinte naranja + ataques básicos no son bloqueados por escudo player.

### 3.2 Backlog lógico pendiente

1. **SetBonusSystem + PlayerStatsComponent leer cósmicos** — wire viento_move_speed_pct_2pc → player.move_speed_mult, luz_passive_hp_regen_2pc → HealthComponent timer, sombra/viento chance mults → HitboxComponent runtime var.
2. **Lanza de Luz Penetrante** (Vael 5to patrón).
3. **Bestiario asignar skill variants a enemies** — qué stages usan Corte Giratorio vs Embestida vs Tajo Doble. Tabla canon por zone + difficulty.
4. **Items dedicados zona 2/3 con elementos canon** (no placeholders).
5. **Tests integración**: enemy state cancel en DESEQUILIBRIO, bendición vampire heal funcional, miasma furia halve in-game.
6. **Reflexión proyectiles** (REQ-INFRA grande) — Boss Lyss Muralla Estática refleja proyectiles.
7. **UI MainMenu selección zona** — wire `StageManager.set_zone()` en menu UI.

### 3.3 Backlog visual (TODAS las zonas)

- Arte IA + sprites + parallax + audio por zona.
- VFX por status (Quemadura llamas, Congelación cristales, Fractura crack, Desequilibrio swirl, Bendición halo, Miasma aura verde).
- HUD chips player skills + cooldowns + Furia meter.
- UI loadout player skills.

---

## 4. Contexto necesario para próxima sesión

### 4.1 Skill dispatch pattern
Si querés agregar más skills variants:
1. Crear `resources/enemy_skills/r2_{clase}_{nombre}.tres` con id, params dict, telegraph/cd/duration.
2. Agregar branches en `enemy.gd._enter_r2_skill_attack` y `_tick_r2_skill_attack` match por `_r2_skill_data.id`.
3. Implementar `_enter_r2_{nombre}` + `_tick_r2_{nombre}(r2_duration)`. Usar helpers `_r2_spawn_aoe_telegraph`, `_r2_apply_radial_damage[_with_status]`, `_r2_param(key, default)`.
4. Para asignar a un enemy: `enemy._r2_skill_data = preload(...)` post-_ready (debug) o agregar override en EnemySpawnEntry.

### 4.2 Projectile source tracking
- `Projectile.source_entity` para LUZ vampire heal. Si null, LUZ no cura.
- Llamar `proj.set_source(self)` post-launch en TODOS los spawn sites nuevos.

### 4.3 Status effects activos
- Catálogo canon: `slow, espiritu_marcial, post_dash_damage, post_dash_invis, burn, stun, fractura (vulnerable.tres), desequilibrio, bendicion, miasma (poison.tres), muralla_estatica`.
- Mismatches filename ↔ id intencionales: `vulnerable.tres → id "fractura"`, `poison.tres → id "miasma"`. Filenames son legacy.

### 4.4 Reglas canon vigentes
- 6 elementos: NEUTRO(0), FUEGO(1), AGUA(2), TIERRA(3), VIENTO(4), LUZ(5), SOMBRA(6).
- Triángulos: primario FUEGO>TIERRA>AGUA>FUEGO + secundario VIENTO>LUZ>SOMBRA>VIENTO. Cross neutral 1.0.
- Element synergy 30% chance: FUEGO=Quemadura, AGUA=Congelación, TIERRA=Fractura single-use, VIENTO=Desequilibrio, LUZ=Bendición vampire, SOMBRA=Miasma bypass+halve furia.
- Bosses state >= 100: 100=Guardian, 200=Duelista, 300=Ignis, 400=Lyss, 500=Vael. Próximos: 600+.
- HurtboxComponent: `bypass_shield = source.ignore_shield` → skip try_absorb.
- ShieldComponent: `consume_charge_force(N)` para skills hostiles.

---

## 5. Archivos clave esta sesión (post-14:28)

### Creados
| Tipo | Archivos |
| :--- | :--- |
| Enemy skills .tres | `resources/enemy_skills/r2_melee_{giratorio,tajo_doble,patada,salto_asalto,arma_imbuida}.tres`, `r2_mage_{nova_hielo,erupcion_terrestre,rafaga_arcana}.tres`, `r2_archer_disparo_reactivo.tres`, `r2_tank_gancho_ascendente.tres` (10) |
| Items | `resources/items/weapons/{arco_huracan,vara_luz,daga_sombra}.tres`, `armor/{manto_viento,casulla_luz,capa_sombra}.tres`, `shields/{paves_viento,halo_luz,orbe_sombra}.tres` (9) |
| Set Bonuses | `resources/set_bonuses/set_bonus_{viento,luz,sombra}.tres` (3) |
| Boss | `scripts/entities/boss_vael.gd` + `scenes/entities/boss_vael.tscn` (2) |
| Tests | `tests/systems/element_status_synergy_test.gd` (1) |

### Modificados
| Archivo | Cambio |
| :--- | :--- |
| `scripts/projectiles/projectile.gd` | source_entity field + set_source API + LUZ vampire heal completo |
| `scripts/entities/{player,enemy,boss_heraldo,boss_lyss,boss_cazadora}.gd` | 13 sites proj.set_source(self) tras launch |
| `scripts/systems/player_skill_system.gd` | proj.set_source(_player) en SPAWN_PROJECTILE |
| `scripts/entities/enemy.gd` | Dispatch por skill_id en _enter/_tick R2 (10 skills branches) + 10 executors + 3 helpers + _tick_arma_imbuida_buff cross-state hook + _physics_process actualizado |
| `scripts/components/hitbox_component.gd` | `ignore_shield: bool` field |
| `scripts/components/hurtbox_component.gd` | bypass_shield si source.ignore_shield |
| `scripts/components/shield_component.gd` | `consume_charge_force(n)` API |
| `scripts/data/stage_data.gd` | `is_mini_boss: bool` field |
| `scripts/data/set_bonus_data.gd` | enum 4-7 + 6 fields cósmicos nuevos |
| `scripts/ui/stage_banner.gd` | branch mini-boss color dorado |
| `resources/stages/zona4_etapa_boss.tres` | boss_scene_override = boss_vael.tscn |
| `resources/stages/zona4_etapa_3.tres` | is_mini_boss = true |
| `.claude/docs/habilidades_generales.md` | §4.1/4.2/4.3 10 skills IMPL marked |

---

## 6. Estado Fase 3 (actualizado al 27/05 15:51)

```
✅ Sistema StatusEffect genérico (12 statuses canónicos)
✅ Element synergy doble eje (natural + cósmico)
✅ 6 elementos canon Leo (FUEGO/AGUA/TIERRA/VIENTO/LUZ/SOMBRA)
✅ EnemySkillData Resource + dispatch por id
✅ 10 skills pool §4 implementadas (variants Set B/C principalmente)
✅ Projectile.source_entity + LUZ vampire heal completo
✅ PlayerSkillSystem (Furia → habilidad, 6 skills + save loadout)
✅ Zona 1 completa (canon GDD)
✅ Zonas 2, 3 lógica completas (materials, stages, bosses Ignis/Lyss)
✅ Zona 4 lógica casi completa (5 stages + mini-boss + boss Vael 4 patrones)
✅ 6 bosses reales (Guardián, Duelista, Cazadora, Heraldo, Ignis, Lyss, Vael)
✅ ShieldComponent.add_temporary_charges + consume_charge_force
✅ HitboxComponent.ignore_shield
✅ HurtboxComponent FRACTURA consume + invuln direccional
✅ 9 items VIENTO/LUZ/SOMBRA dedicated
✅ 3 set bonuses cósmicos data canónica
✅ Mini-boss stage type + banner UI
✅ World/StageManager multi-zona

⏹ SetBonusSystem + PlayerStatsComponent leer fields cósmicos (data sin runtime)
⏹ Lanza de Luz Penetrante (Vael 5to patrón REQ-INFRA Line2D tracking)
⏹ Bestiario asignar skill variants a enemies (cuál stage usa cuál)
⏹ Reflexión proyectiles (REQ-INFRA grande)
⏹ Visual TODAS las zonas (arte/sprites/parallax/audio/VFX)
⏹ HUD player skills + Furia meter
⏹ UI MainMenu selección zona
⏹ Items dedicados FUEGO/AGUA per-zone (no solo placeholders zona 2/3)
⏹ GDD §5.3 sincronizar 6 elementos
```

48 tasks acumuladas (39 sesión 14:28 + 9 esta sesión post). Sistema lógico Fase 3 prácticamente cerrado. Próximo paso: validación in-game + visual.
