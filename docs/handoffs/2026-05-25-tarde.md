# Handoff — 25 de Mayo 2026, sesión maratón Fase 2

**Sesión:** Apertura de Fase 2 + Milestone A drops + diferenciación R1/R2/R3 + Refinamiento backend + Crafteo backend + distribución de rarezas + bestiario + intento de boss R4 (cortado por límite).
**Fase del proyecto:** Fase 2 — Loop Básico (abierta).
**GDD versión vigente:** v2.1.
**Generado por:** Claude principal (manualmente — `/genera-resumen` no se usó).
**Continúa desde:** `docs/handoffs/2026-05-24-2303.md` (cierre Fase 1).

---

## ⚠ INSTRUCCIÓN AL ARRANCAR LA PRÓXIMA SESIÓN

**El juego estaba roto en el último intento de playtest.** Diagnóstico aplicado: los archivos del boss R4 incompletos (T15, sesión cortada por límite) fueron movidos a `_wip/` para descartarlos como causa. **Pendiente validar en Godot que ahora carga.**

**Primer paso al arrancar:**
1. Abrir Godot, intentar correr el proyecto.
2. Si carga: confirmás que los archivos del boss eran la causa. Procedés con el playtest de Milestone A drops + 3 enemies R1/R2/R3 + zonas E1/E2/E3.
3. Si NO carga: hay otra causa. Reportá el error de la consola de Godot.

---

## 1. Qué se hizo en esta sesión (gigante)

### T1-T7 — Milestone A: drops de materiales por kill ✅
- 4 materiales (`piedra_resonancia`, `hierba_antigua`, `savia_resonante`, `esencia_verdor`) con naming + descripciones canónicas.
- `MaterialData`, `DropEntry`, `DropTable` (Resources). `DropTable.roll(momentum)` con fórmula GDD §111.
- `DropSystem` autoload — hook a `HealthComponent.died`, fallback a `StageData.material_drops`.
- `InventorySystem` extendido con `_materials` dict + `add_material` / `remove_material` / `get_material_count`.
- 4 drop tables por stage (`zona1_etapa_<N>_materials.tres`).
- `MaterialToastContainer` + `MaterialToast` UI en hud_combat top-right con stacking.
- Wiring en `world.gd._spawn_stage` con `DropSystem.register_enemy(enemy, entry.material_drops)`.
- Balance audit por `balance-engineer`: PASS, sin ajustes obligatorios.
- 9 tests unitarios (`tests/systems/drop_table_test.gd`).
- 3 bugs CRITICAL fixeados en round 3: double-connect, ref colgante (`tree_exited` hook), Game Over reset (`DropSystem.reset()` llamado desde `game_over_screen.gd`).
- 6 tests integración (`tests/systems/drop_system_integration_test.gd`).
- Doc completo en `docs/features/equipment/drops_milestone_a.md`.

### T8 — 3 enemies R1/R2/R3 diferenciados ✅
- Behaviors según GDD §7.3:
  - R1: sin cambios.
  - R2: bloqueo via `hurtbox.invulnerable=true` + aura azul (40% chance, cooldown 2.5s).
  - R3: dodge dash lateral (25% chance, cooldown 4s) + skill especial telegrafiada 1.5s con daño ×1.5.
- Visual: tints + scale + aura partículas para R3 (sin sprites nuevos).
- 3 drop tables por rareza: `resources/enemies/drop_tables/enemy_r{1,2,3}_materials.tres`.
- Stage E2 modificada con mix `[Melee R1×2, Tank R2×1, Melee R3×1]`.
- Doc en `docs/features/enemies/rareza_diferenciacion.md`.

### T9 — Fixes inline post-T8 ✅
- Nerf R3 aplicado al `enemy_r3_materials.tres`:
  - `hierba_antigua` drop_chance 0.30 → 0.25.
  - `savia_resonante` count_max 2 → 1.
  - `piedra_resonancia` 0.30 → 0.20, count_max 2 → 1.
  - `esencia_verdor` 0.20 → 0.10.
- Dist-check del skill R3 en `enemy.gd`: `dist < detect_range * 0.6` (antes era `dist < detect_range`) para evitar telegraph "desde lejos".
- `docs/sugerencias.md` creado para acumular ideas futuras.
- `docs/features/phase_1_retrospective.md` creado.

### T10 — Refinamiento backend ✅
- `UpgradeManager` autoload con `REFINE_TABLE` (GDD §5.6 fiel).
- `RefineResult` Resource (success, previous_level, new_level, materials_consumed, gold_consumed, protected_by_scroll, dropped_to_level, penalty_type).
- API: `can_refine / get_target_level / get_success_chance / get_penalty_type / get_cost / requires_scroll_to_protect / can_use_scroll / attempt_refine`.
- `pergamino_proteccion.tres` creado (R3, max_stack 99).
- Signals para UI futura: `refine_started / refine_succeeded / refine_failed / refine_aborted`.
- 12 tests (`tests/systems/upgrade_manager_test.gd`).
- Doc en `docs/features/equipment/refinement_backend.md`.
- **UI pendiente** (requiere playtest para feel).
- TODO oro: dependency injection lista (`gold_cost=0` por ahora).

### T11 — Tests integración DropSystem ✅
- 6 tests cubriendo los 3 bugs CRITICAL + fallback + end-to-end (`tests/systems/drop_system_integration_test.gd`).
- Modificaciones mínimas a `drop_system.gd`: agregados `_test_registered_count()` y `_test_is_registered()` para testabilidad sin tocar comportamiento.

### T12 — Crafteo backend ✅
- `CraftingSystem` autoload — `try_craft(recipe)` atómico.
- `CraftRecipe`, `CraftRecipeInput`, `CraftResult` Resources.
- 5 recetas iniciales en `resources/recipes/`:
  - `craft_cota_cuero` (R1): 8 Hierba + 2 Savia → Cota de Cuero.
  - `craft_espada_hierro` (R2): 5 Hierba + 2 Savia + 1 Piedra → Espada de Hierro.
  - `craft_escudo_hierro` (R2): 4 Savia + 2 Piedra → Escudo de Hierro.
  - `craft_vara_cristal` (R3): 3 Savia + 2 Esencia + 3 Piedra → Vara de Cristal.
  - `craft_martillo_guardian` (R3): 6 Hierba + 4 Savia + 2 Esencia + 2 Piedra → Martillo del Guardián.
- 12 tests (`tests/systems/crafting_system_test.gd`).
- Doc en `docs/features/equipment/crafting_backend.md`.
- **UI pendiente.**
- TODO oro: igual que Refinamiento.

### T13 — Distribución de rarezas ✅
- `zona1_etapa_1.tres`: `[Melee R1×3, Archer R1×2]` — introducción suave.
- `zona1_etapa_2.tres`: intacta (ya tenía mix R1/R2/R3).
- `zona1_etapa_3.tres`: `[Melee R1×1, Tank R2×2, Archer R2×1, Mage R3×1]` — R2 dominante.
- `zona1_etapa_4.tres`: intacta (es el boss, `is_boss=true`).
- Doc en `docs/features/zones/valle_de_los_ecos_pacing.md`.

### T14 — Fixes admin ✅
- `StageManager.reset()`: agregada `_is_pending = false` (bug detectado por godot-expert).
- `.claude/docs/glossary.md`: expandida entrada Piedra de Resonancia + 3 entradas para los nuevos materiales reales (Hierba Antigua, Savia Resonante, Esencia del Verdor) + nota sobre los materiales del lore previo no implementados.

### T15 — Boss R4 "Guardián de la Maleza" ⚠ CORTADA + ARCHIVOS MOVIDOS A _wip/
- `boss-designer` alcanzó a crear 2 archivos antes del límite de sesión:
  - `scripts/entities/boss_guardian.gd` (571 líneas, extends Enemy con state machine extendida).
  - `scripts/components/enemy_shield_component.gd` (73 líneas).
- **Ambos archivos están en `_wip/`** (junto con sus `.uid`) para que Godot NO los cargue.
- 0 refs externas a `BossGuardian` o `EnemyShieldComponent` en el proyecto activo — moverlos no rompe nada.
- E4 sigue spawneando un Tank R4 placeholder (el boss original).
- **Decisión pendiente:** re-implementar el boss desde cero más cuidadosamente (chunks, validación al final de cada paso) o adoptar `_wip/boss_guardian.gd` con review humano.

### T16 — Bestiario Zona 1 ✅ (parcial completado por Claude principal)
- `narrative-lore` alcanzó a crear `docs/lore/bestiario_zona_1.md` antes del límite (7 especies + boss, tiers 1/2/3, contra-estrategias).
- Los 2 docs faltantes los escribió Claude principal post-limite:
  - `docs/lore/valle_de_los_ecos.md` — contexto narrativo de la zona.
  - `docs/lore/materiales_zona_1.md` — descripciones extendidas + flavor toasts.

### Meta — Regla 6 nueva
- Agregada a `CLAUDE.md §2 Regla 6 — Delegar a subagentes especializados por default`.
- Documentada en `.claude/docs/reglas-de-trabajo.md §6` + tabla mapeo dominio→agente + ejemplos.
- Memoria persistente: `~/.claude/projects/.../memory/feedback_delegar_a_subagentes.md`.
- Estado: 7 reglas operativas en total.

### Meta — Memoria persistente
- `feedback_priorizar_fase_1.md` archivado.
- `feedback_priorizar_fase_2.md` activo (empujar a cerrar Fase 2).
- `feedback_delegar_a_subagentes.md` activo.

---

## 2. En qué estamos ahora

- **Fase y hito:** Fase 2 — Loop Básico. Hito: *"el loop pelear → lootear → mejorar engancha"*.
- **Última feature tocada:** Boss R4 (T15 cortada — archivos en `_wip/`).
- **Estado del repo:** sin git inicializado. Working tree con muchos cambios.
- **Tests creados esta sesión:** 9 (drop_table) + 6 (drop_system integration) + 12 (upgrade_manager) + 12 (crafting_system) = **39 tests nuevos**.
- **Tests NO corridos automáticamente.** Pendiente verificar manualmente en Godot.
- **Decisiones pendientes (esperan a Leo):**
  - Si el juego carga sin el boss → confirmar diagnóstico + proceder con playtest.
  - Si NO carga → reportar error de Godot para investigar.
  - Decidir destino del boss R4: re-implementar desde cero o adoptar `_wip/`.
  - Decidir si aplicar nerf adicional al R3 si en playtest se siente "spammy" (sugerencia balance-engineer: bajar `esencia_verdor` en E4 fallback de 0.10 a 0.08).
  - Decidir si crear UI de Refinamiento o de Crafteo (ambos backends listos, esperando UI).
  - Resolver inconsistencia de nomenclatura: bestiario narrativo (Vigía Roto / Heraldo Vacío / etc) vs GDD §10 original (Guerreros de corteza / Chamanes de espinas).

---

## 3. Qué sigue

### Próximo paso inmediato (Leo en Godot)
1. **Abrir Godot y correr el proyecto.** Validar que carga sin errores.
2. Si carga: jugar Etapa 1 → 2 → 3 → 4 (boss tank placeholder).
3. Smoke checks priorizados:
   - Toasts de material aparecen en HUD superior derecho.
   - R2 (Tank, tinte azul) ocasionalmente bloquea con aura azul.
   - R3 (Melee, tinte violeta) dashea lateral + skill telegrafiada 1.5s.
   - Drops por kill: R1 → hierba/piedra; R3 → savia/esencia (raro).
   - Game Over → Reintentar limpia inventario y stage_pending sin errores.

### Backlog corto si playtest sale bien
- **UI Refinamiento** (delegar a `ux-mobile`): panel con probabilidades visibles, animación de intento, slot para Pergamino. Conectar a signals de UpgradeManager.
- **UI Crafteo** (delegar a `ux-mobile`): lista de recetas conocidas, indicador de materiales faltantes, botón craftear. Conectar a CraftingSystem.
- **UI inventario de materiales** (extender InventoryScreen): tab o sección para mostrar materiales con count + tooltips.
- **Boss R4 re-implementación** (delegar a `boss-designer` con cuidado): re-ver `_wip/boss_guardian.gd`, validar arquitectura, dividir en chunks. Validar parse en cada paso.
- **Milestone B drops** (loot card al stage cleared, items equipables): cuando el loop core esté validado.

### Cosas a verificar manualmente (sin Godot, alguien puede leer)
- `docs/lore/valle_de_los_ecos.md` y `bestiario_zona_1.md`: ¿el tono es consistente con la fantasía? ¿Inconsistencia con GDD §10 sobre nomenclatura?
- `docs/sugerencias.md`: 11 ideas registradas. Revisar cuáles entran al GDD y cuáles se archivan.

---

## 4. Contexto necesario para la próxima sesión

### Archivos archivados en `_wip/`
- `_wip/boss_guardian.gd` — 571 líneas, **POSIBLEMENTE FUNCIONAL** pero no validado.
- `_wip/enemy_shield_component.gd` — 73 líneas, clean.
- `_wip/boss_guardian.gd.uid` y `_wip/enemy_shield_component.gd.uid` — los .uid también.

Si Leo decide adoptarlos, **moverlos de vuelta a su path original** (`scripts/entities/` y `scripts/components/`), abrir Godot y validar que el editor no reporta errores ANTES de intentar instanciar.

### Lo que NO se hizo (por orden o por límite)
- Git no se inicializó (decisión pendiente de Leo).
- Boss R4 no se conectó a E4 — sigue spawneando Tank R4 placeholder.
- Drop table específica del boss no se creó (E4 usa el `zona1_etapa_4_materials.tres` que ya existía).
- Ningún test fue corrido (no hay Godot CLI disponible en el entorno de la sesión).
- UI de Refinamiento/Crafteo no se implementó (requiere playtest).

### Regla nueva (Regla 6)
- **Cualquier tarea con dominio en `.claude/agents/` se delega vía Agent.** Claude principal orquesta, no absorbe.
- Aplicó esta sesión: 9 agentes invocados (narrative-lore, equipment-system ×3, ux-mobile, balance-engineer ×2, godot-expert ×2, enemy-ai, level-designer, boss-designer).

### Lección importante de esta sesión
- **Tras delegar tarea grande de código (>200 líneas), validar al menos parse-time en Godot ANTES de seguir.** Si la sesión muere a mitad de un Agent, el código queda en estado desconocido.
- **No moverse a la siguiente task antes de confirmar que la anterior compila.** En esta sesión, lanzamos 3 agentes en paralelo (boss + bestiario + admin) y el boss falló sin que nos enteráramos hasta el playtest.

---

## 5. Archivos clave de esta sesión

### Nuevos sistemas (autoloads)
| Archivo | Razón |
|---|---|
| `scripts/systems/drop_system.gd` | Autoload de drops por kill. |
| `scripts/systems/upgrade_manager.gd` | Autoload de refinamiento +1 a +10. |
| `scripts/systems/crafting_system.gd` | Autoload de crafteo. |

### Nuevas clases de datos
| Archivo | Razón |
|---|---|
| `scripts/data/material_data.gd` | Materiales (no equipables, fungibles). |
| `scripts/data/drop_entry.gd` | Entry de drop table (item|material). |
| `scripts/data/drop_table.gd` | Tabla de drops con `roll(momentum)`. |
| `scripts/data/refine_result.gd` | Resultado de refinamiento. |
| `scripts/data/craft_recipe.gd` | Receta de crafteo. |
| `scripts/data/craft_recipe_input.gd` | Input de receta. |
| `scripts/data/craft_result.gd` | Resultado de crafteo. |

### UI nueva
| Archivo | Razón |
|---|---|
| `scripts/ui/material_toast.gd` + `.tscn` | Toast individual de material recogido. |
| `scripts/ui/material_toast_container.gd` + `.tscn` | Container con stacking. |

### Modificados
| Archivo | Cambio |
|---|---|
| `scripts/systems/inventory_system.gd` | +materiales API, +signal material_removed, +reset(quiet), +guards count<=0. |
| `scripts/systems/stage_manager.gd` | reset() resetea `_is_pending`. |
| `scripts/entities/enemy.gd` | +R2 bloqueo, +R3 dodge+skill, +dist-check skill, +tints/scale por rareza. |
| `scripts/data/enemy_spawn_entry.gd` | +`material_drops: DropTable` opcional. |
| `scripts/data/stage_data.gd` | +`material_drops: DropTable` fallback. |
| `scripts/world/world.gd` | +llamada a `DropSystem.register_enemy` paralelo a `StageManager.register_enemy`. |
| `scripts/ui/game_over_screen.gd` | +`DropSystem.reset()` en Reintentar. |
| `scenes/ui/hud_combat.tscn` | +MaterialToastContainer en CanvasLayer. |
| `project.godot` | +autoloads DropSystem, UpgradeManager, CraftingSystem. |
| `resources/stages/zona1_etapa_1.tres` | composición + material_drops por entry. |
| `resources/stages/zona1_etapa_2.tres` | composición mix R1/R2/R3 + material_drops. |
| `resources/stages/zona1_etapa_3.tres` | composición R2 dominante + material_drops. |
| `.claude/docs/reglas-de-trabajo.md` | +Regla 6 delegar. |
| `CLAUDE.md` | §2 +Regla 6, §4 actualizado Fase 2. |
| `.claude/docs/glossary.md` | expandida Piedra Resonancia + 3 materiales nuevos. |
| `.claude/docs/fases.md` | estado actual Fase 2. |

### Recursos `.tres` nuevos
- 5 materiales: `resources/materials/piedra_resonancia.tres`, `hierba_antigua.tres`, `savia_resonante.tres`, `esencia_verdor.tres`, `pergamino_proteccion.tres`.
- 4 drop tables por stage: `resources/stages/drop_tables/zona1_etapa_{1-4}_materials.tres`.
- 3 drop tables por rareza: `resources/enemies/drop_tables/enemy_r{1-3}_materials.tres`.
- 5 recetas: `resources/recipes/craft_{cota_cuero,espada_hierro,escudo_hierro,vara_cristal,martillo_guardian}.tres`.

### Tests
- `tests/systems/drop_table_test.gd` (9 casos).
- `tests/systems/drop_system_integration_test.gd` (6 casos).
- `tests/systems/upgrade_manager_test.gd` (12 casos).
- `tests/systems/crafting_system_test.gd` (12 casos).

### Docs
- `docs/features/equipment/drops_milestone_a.md`
- `docs/features/equipment/refinement_backend.md`
- `docs/features/equipment/crafting_backend.md`
- `docs/features/enemies/rareza_diferenciacion.md`
- `docs/features/zones/valle_de_los_ecos_pacing.md`
- `docs/features/ui/material_toast.md`
- `docs/features/phase_1_retrospective.md`
- `docs/lore/bestiario_zona_1.md`
- `docs/lore/valle_de_los_ecos.md`
- `docs/lore/materiales_zona_1.md`
- `docs/sugerencias.md`
